# D&D Codebase Deep Dive Audit Reports

Generated by 10 parallel analysis agents on 2025-12-09.

---


---

Now let me create a comprehensive audit report based on my analysis:

## DEEP DIVE AUDIT REPORT: /opt/dnd/complete-intelligent-server.js

### File Overview
- **Size**: 280KB / 6,924 lines
- **Purpose**: Main D&D campaign server with AI integration, combat management, and multi-campaign support

---

## 🔴 CRITICAL ISSUES

### 1. **Synchronous File System Operations (Performance/Blocking)**

**Lines 52-54**: Synchronous file read in constructor
```javascript
if (fs.existsSync(settingsPath)) {
    const settings = JSON.parse(fs.readFileSync(settingsPath, 'utf8'));
    return settings.currentProvider;
```
**Issue**: Blocks event loop during class initialization. Should use async/await pattern.

**Line 73**: Synchronous write in hot path
```javascript
fs.writeFileSync(settingsPath, JSON.stringify(settings, null, 2));
```
**Issue**: Blocks event loop every time AI provider is changed.

**Line 717**: API key loading blocks initialization
```javascript
const config = JSON.parse(fs.readFileSync('./api-config.json', 'utf8'));
```
**Issue**: Called in `getApiKey()` which is in critical path for every AI request.

**Line 5049**: Campaign HTML routing synchronous check
```javascript
if (fs.existsSync(campaignHtmlPath)) {
```
**Issue**: Blocks request handling. Should use async fs.access() instead.

---

### 2. **Hardcoded Campaign References (Should Be Dynamic)**

**19 instances of 'test-silverpeak' hardcoded**, creating tight coupling:

**Lines 993-1020**: Database initialization hardcoded to test-silverpeak
```javascript
if (this.campaignId === 'test-silverpeak') {
    console.log('📊 Initializing database for Silverpeak...');
```
**Issue**: Database/RAG features only work for one campaign. Should check campaign config.

**Lines 2376-2383**: Monster caching only for test-silverpeak
```javascript
if (this.memoryClient && this.campaignId === 'test-silverpeak') {
    await this.memoryClient.storeMonsterStats(name, monsterData);
```
**Issue**: Memory optimization unavailable for other campaigns.

**Lines 2754-2766, 3215-3223**: RAG memory only for test-silverpeak
```javascript
if (this.campaignId === 'test-silverpeak' && this.memoryClient) {
    const memories = await this.memoryClient.retrieveMemories(playerAction, 5);
```
**Issue**: Advanced memory features locked to one campaign.

**Lines 3554-3562, 5735-5741, 5875-5881**: Database persistence only for test-silverpeak
```javascript
if (this.campaignId === 'test-silverpeak' && this.db) {
    await this.applyStateToDB(changes);
```
**Issue**: State persistence fails for other campaigns.

**Lines 6532-6842**: 8 API endpoints restrict to 'test-silverpeak'
```javascript
if (campaign !== 'test-silverpeak') {
    return res.status(400).json({
        error: 'Equipment API only available for fantasy campaigns'
    });
}
```
**Issue**: Equipment, HP, credits, conditions APIs fail for all other campaigns.

**Line 3915**: Default campaign hardcoded
```javascript
const DEFAULT_CAMPAIGN = process.env.DEFAULT_CAMPAIGN || 'test-silverpeak';
```
**Issue**: Should be 'default' or dynamically determined.

---

### 3. **Race Conditions & Missing Awaits**

**Lines 2610-2611**: State update without await in critical path
```javascript
await this.updateMemory(playerAction, response, sessionId, mode);
// Combat turn advance code follows immediately
```
**Issue**: updateMemory is async but following code assumes it's complete.

**Lines 3027-3036**: Array operations without await in loop
```javascript
newEvent.metadata.keywords.forEach(keyword => {
    if (!this.searchIndices.keywords[keyword]) {
        this.searchIndices.keywords[keyword] = [];
    }
    this.searchIndices.keywords[keyword].push(newEvent.index);
});
```
**Issue**: Modifying shared state (searchIndices) without locks, concurrent requests can corrupt data.

**Lines 5097-5121**: Roll queue enqueue in parallel without transaction
```javascript
for (const rollRequestText of rollRequests) {
    const queuedEntry = await enqueueRollRequestFromNarrative(...);
    if (queuedEntry) {
        queuedRollEntries.push(queuedEntry);
    }
}
```
**Issue**: Multiple async operations modifying combatState without atomicity guarantees.

**Lines 4800-4808**: Await in participant result but state already partially modified
```javascript
await persistCombatStateWithQueue(context, campaignId, combatState);
console.log('🎲 [ROLL QUEUE] Awaiting damage roll');
return entry;
```
**Issue**: Early return after persist but before status update completes.

---

### 4. **Error Handling Gaps**

**76 catch blocks** in file, but many swallow errors:

**Lines 56-59**: Silent error suppression
```javascript
} catch (error) {
    console.log('No saved AI provider settings found, using default');
}
return null;
```
**Issue**: Loses error context, hard to debug. Should log error details.

**Lines 726-728**: Falls through without logging specifics
```javascript
} catch (err) {
    // Fall through to environment lookup below
}
```
**Issue**: Silent failure makes debugging impossible.

**Lines 1000-1003**: Database init failure falls back silently
```javascript
} catch (error) {
    console.error('❌ Failed to initialize database:', error);
    console.error('   Falling back to JSON state management');
    this.db = null;
}
```
**Issue**: Should throw or return error status, not continue in degraded mode.

**Lines 1016-1019**: RAG service failure swallowed
```javascript
} catch (error) {
    console.error('❌ Failed to connect to RAG service:', error);
    this.memoryClient = null;
}
```
**Issue**: System continues without critical feature, users unaware.

**Lines 2298-2301**: Enemy extraction errors suppressed
```javascript
} catch (error) {
    console.error('❌ Error extracting enemy data:', error);
    return null;
}
```
**Issue**: Combat initiation can fail silently, leaving game in broken state.

**Lines 2762-2765**: Memory retrieval failures hidden
```javascript
} catch (error) {
    console.warn('⚠️  Failed to retrieve RAG memories:', error.message);
    // Continue without memories
}
```
**Issue**: Degraded AI responses without user notification.

---

### 5. **Memory Leaks**

**Lines 1288, 1321, 1328**: Unbounded array growth
```javascript
this.indexedEvents.push(currentEvent);
```
**Issue**: indexedEvents array grows indefinitely until trimmed at line 3041 (only every 10 entries).

**Lines 3041-3063**: Partial trimming solution
```javascript
const MAX_INDEXED_EVENTS = 500;
if (this.indexedEvents.length > MAX_INDEXED_EVENTS) {
    console.log(`🧹 Trimming indexedEvents from ${this.indexedEvents.length} to ${MAX_INDEXED_EVENTS}`);
```
**Issue**: Trim happens too late, memory spikes before cleanup. Should be proactive.

**Lines 1446-1456, 1475-1495**: Search indices grow unbounded
```javascript
this.searchIndices.keywords[keyword].push(event.index);
this.searchIndices.npcs[npc].push(event.index);
this.searchIndices.locations[location].push(event.index);
```
**Issue**: Maps grow forever, no cleanup when events are trimmed. Orphaned references.

**Lines 3965-3970**: Roll queue constants but no enforcement
```javascript
const DEFAULT_ROLL_QUEUE_LIMIT = 50;
```
**Issue**: Limit defined but trimRollQueue (lines 4035-4047) doesn't enforce it properly.

**Lines 5923-5936**: Combat conversation history unbounded
```javascript
const conversationHistory = combatState.conversationHistory || [];
// ... later ...
conversationHistory.push({ role: 'user', content: action });
conversationHistory.push({ role: 'assistant', content: response });
```
**Issue**: No limit on combat conversation size, can grow indefinitely during long battles.

**Line 2307-2308**: Monster cache never cleared
```javascript
const cache = this.monsterCache || new Map();
this.monsterCache = cache;
```
**Issue**: Cache persists forever, no eviction policy.

---

### 6. **Dead Code Paths**

**Lines 1280-1283**: Unreachable chapter detection logic
```javascript
if (line.includes('[END OF CHAPTER 1: THE WANDERER]') ||
    line.includes('CHAPTER 2: TITAN STATION')) {
    isHistorical = false;
```
**Issue**: parseIntoEvents called on emergency export that never contains these markers.

**Lines 2424-2436**: generateEmergencyExportFromConversation defined but not used
```javascript
generateEmergencyExportFromConversation(conversationHistory) {
    let markdown = `# EMERGENCY CAMPAIGN EXPORT - ${new Date().toISOString().split('T')[0]}\n\n`;
```
**Issue**: Only called in rollback endpoint (line 6151) which may never execute.

**Lines 3210-3212**: Commented-out code
```javascript
// The handleCombatTurnAnnouncements logic is now handled by processPlayerAction
// await this.handleCombatTurnAnnouncements(dmResponse);
```
**Issue**: Dead code should be removed, creates confusion.

**Lines 5625-5668**: Massive fallback logic that duplicates startCombat
```javascript
} catch (error) {
    console.error('❌ Combat start via handoff failed, falling back to manual initialization:', error);
    // 40 lines of fallback code
```
**Issue**: Duplicate logic makes maintenance difficult. Should fix root cause.

---

### 7. **API Endpoint Mismatches**

**Lines 5184-5189**: Multiple route aliases
```javascript
const actionRoutes = [
    '/api/dnd/action',
    '/dnd-api/dnd/action',
    '/dnd/api/dnd/action'
];
```
**Issue**: Three different paths for same endpoint creates confusion. Frontend may expect different path.

**Lines 5289-5463**: Roll queue has 3 route aliases but complex nested endpoints
```javascript
rollQueueRoutes.forEach(route => {
    app.get(route, async (req, res) => { ... });
    app.post(route, async (req, res) => { ... });
    app.post(`${route}/:queueId/resolve`, async (req, res) => { ... });
```
**Issue**: Creates 9 total endpoints (3 x 3), hard to maintain consistency.

**Lines 5549-5557**: Combat state has 6 different route aliases
```javascript
const combatStateRoutes = [
    '/api/dnd/combat/state',
    '/api/dnd/combat-state',
    '/dnd-api/dnd/combat/state',
    '/dnd-api/dnd/combat-state',
    '/dnd/api/dnd/combat/state',
    '/dnd/api/dnd/combat-state'
];
```
**Issue**: Combinatorial explosion - 6 aliases for one resource.

**Lines 5078-5100**: Parameter inconsistency
```javascript
const { action, character, campaignState, sessionId, useRealClaude, mode, campaignId, campaign } = req.body;
```
**Issue**: Accepts both 'campaignId' and 'campaign', creates ambiguity. Pick one.

---

## ⚠️ MODERATE ISSUES

### 8. **Deprecated Patterns**

**Line 14**: node-fetch v2 usage
```javascript
const fetch = require('node-fetch');
```
**Issue**: Node 18+ has native fetch. No need for polyfill.

**Lines 775-778, 844-857**: Hardcoded AI instructions in provider classes
```javascript
const enhancedSystem = system + `
CRITICAL: You are the Dungeon Master. Follow the INTELLIGENT DICE ROLL SYSTEM...
```
**Issue**: System prompt hardcoded in multiple places. Should be configuration.

**Lines 6229**: bcrypt usage without salt rounds config
```javascript
const bcrypt = require('bcrypt');
```
**Issue**: Using bcrypt correctly but no visible salt configuration.

---

### 9. **Code Smell: Function Length**

**Lines 982-1026**: initialize() method 45 lines, too complex
**Lines 1522-1608**: retrieveRelevantContext() 86 lines, does too much
**Lines 2072-2302**: extractEnemyData() 230 lines, mega-function
**Lines 2544-2746**: processPlayerAction() 202 lines, needs refactoring
**Lines 3239-3347**: extractStateChanges() 108 lines of AI prompt
**Lines 3349-3570**: applyStateChanges() 221 lines, complex branching
**Lines 5596-5751**: Combat start endpoint 155 lines, too much logic

---

### 10. **Security Concerns**

**Line 6383**: Hardcoded API key (Stability AI)
```javascript
const STABILITY_API_KEY = 'ASK_CHRIS_FOR_NEW_STABILITY_KEY';
```
**⚠️ CRITICAL SECURITY ISSUE**: API key exposed in code. Should use environment variable.

**Line 6252**: Simple token generation
```javascript
const token = Buffer.from(`${id}:${Date.now()}`).toString('base64');
```
**Issue**: Predictable token, not cryptographically secure. Should use JWT or crypto.randomBytes.

**Lines 5028-5037**: No request size validation before JSON parse
```javascript
app.use(express.json({
    limit: '50mb',
```
**Issue**: Allows 50MB JSON payloads without rate limiting. DoS risk.

---

## 📊 SUMMARY STATISTICS

- **Total Lines**: 6,924
- **Synchronous Operations**: 5 critical paths
- **Hardcoded Campaigns**: 19 instances of 'test-silverpeak'
- **Catch Blocks**: 76 (many silently swallow errors)
- **Memory Leak Vectors**: 8 identified
- **Dead Code Sections**: 4 major blocks
- **API Route Aliases**: 30+ duplicate routes
- **Mega Functions**: 7 functions over 100 lines
- **Security Issues**: 2 critical (hardcoded key, weak auth)

---

## 🎯 PRIORITY RECOMMENDATIONS

### P0 (Fix Immediately)
1. **Remove hardcoded Stability API key** (line 6383) - security breach
2. **Fix test-silverpeak hardcoding** - breaks multi-campaign support
3. **Convert sync fs to async** (lines 52-54, 73, 717, 5049)

### P1 (Fix Soon)
4. **Add array bounds enforcement** - prevent memory leaks (lines 1288, 3041, 5923)
5. **Improve error handling** - stop swallowing errors (76 catch blocks)
6. **Fix race conditions** - add locks for shared state (line 3027-3036)

### P2 (Technical Debt)
7. **Consolidate API routes** - reduce from 30+ to standard set
8. **Refactor mega-functions** - break down 200+ line functions
9. **Remove dead code** - clean up unreachable paths

---

This audit reveals a functional but brittle codebase with significant technical debt that will cause production issues under load.


---

Now let me create a comprehensive audit report based on my analysis:

---

## **FRONTEND AUDIT REPORT: /opt/dnd/script.js**

### **CRITICAL ISSUES**

---

### **1. RACE CONDITIONS IN ASYNC OPERATIONS**

#### **Issue 1.1: Multiple Auto-Sync Race Condition**
**Lines: 68-84**
```javascript
startAutoSync() {
    // Clear any existing sync interval
    if (this.syncInterval) {
        clearInterval(this.syncInterval);
    }

    // Set up 30-second polling
    this.syncInterval = setInterval(async () => {
        try {
            await this.syncWithDM(true); // Silent sync
        } catch (error) {
            console.error('❌ Auto-sync failed:', error);
        }
    }, 30000); // 30 seconds
```
**Problem:** If `init()` fails and is retried, or if the page is reloaded quickly, multiple sync intervals could be running simultaneously before the old one is cleared.

---

#### **Issue 1.2: Post-Action Sync Race Condition**
**Lines: 324-331**
```javascript
// Sync with DM after message to get any state changes
setTimeout(async () => {
    try {
        await this.syncWithDM(true); // Silent sync after action
    } catch (error) {
        console.error('❌ Post-action sync failed:', error);
    }
}, 1000); // Small delay to let server process
```
**Problem:** This setTimeout-based sync can overlap with the auto-sync (line 75-81) and the `switchPartyMember` sync (line 1486), potentially causing:
- Multiple simultaneous fetch requests to `/dnd-api/dnd/state`
- DOM updates happening out of order
- Stale data overwriting fresh data

---

#### **Issue 1.3: Claude API Initialization Race**
**Lines: 39-46**
```javascript
try {
    await window.claudeAPI.initialize();
    console.log('⚔️ Claude DM: Ready for adventure with Claude API!');
} catch (error) {
    console.error('Failed to initialize Claude API:', error);
    this.addLogEntry('dm', '⚠️ Error: Could not connect to Claude API...');
}
```
**Problem:** No check if `window.claudeAPI` exists before calling `initialize()`. If `claude-api.js` fails to load, this throws an uncaught error.

---

### **2. HARDCODED CAMPAIGN REFERENCES**

#### **Issue 2.1: Hardcoded 'dax' in Initialization**
**Line 28**
```javascript
this.switchInventoryTab('dax'); // Initialize with Dax's inventory
```
**Problem:** Should use `window.campaignId` or dynamic detection instead of hardcoding 'dax'.

---

#### **Issue 2.2: Hardcoded Character in loadFromURL**
**Line 1437**
```javascript
const character = urlParams.get('character') || 'dax'; // Default to Dax
```
**Problem:** Defaults to 'dax' instead of using `window.campaignId` or campaign-specific default.

---

#### **Issue 2.3: Hardcoded Inventory Initialization**
**Lines 1886-1891**
```javascript
if (!this.characterInventories) {
    this.characterInventories = {
        dax: ['Engineering Kit'],
        chen: [],
        yuen: []
    };
}
```
**Problem:** Hardcoded character keys ('dax', 'chen', 'yuen') instead of loading from campaign configuration.

---

#### **Issue 2.4: Hardcoded Character Keys in getCharacterPreset**
**Lines 1490-1551**
```javascript
const presets = {
    dax: { name: 'Dax Stargazer', ... },
    chen: { name: 'Chen', ... },
    yuen: { name: 'Dr. Yuen', ... }
};
```
**Problem:** Entire preset system is hardcoded to these three characters. Should be loaded from server based on `window.campaignId`.

---

### **3. API ENDPOINT ISSUES**

#### **Issue 3.1: Mismatched API Path Prefixes**
**Lines 743, 1139, 1169, 1586, 2016**
```javascript
// Line 743 - Uses /dnd/ prefix
const campaignPath = window.campaignId ? `/dnd/campaigns/${window.campaignId}/conversation-history.json` : './conversation-history.json';

// Line 1139 - Uses /dnd/api/ prefix
const response = await fetch('/dnd/api/dnd/ai-provider');

// Line 1586 - Uses /dnd-api/ prefix (different!)
const response = await fetch(`/dnd-api/dnd/state${campaignParam}`);

// Line 2016 - Uses /dnd-api/ prefix
const response = await fetch('/dnd-api/dnd/generate-scene', {
```
**Problem:** Inconsistent API path prefixes:
- `/dnd/campaigns/` (line 743)
- `/dnd/api/dnd/` (lines 1139, 1169)
- `/dnd-api/dnd/` (lines 1586, 2016)

This suggests path mismatches that could cause 404 errors.

---

#### **Issue 3.2: Missing Error Handling for Conversation History**
**Lines 743-756**
```javascript
const response = await fetch(campaignPath);
if (response.ok) {
    const serverConversation = await response.json();
    // Convert...
} else {
    throw new Error('Enhanced server conversation not available');
}
```
**Problem:** If the fetch throws (network error), it's not caught here. Only checks `response.ok`, not fetch failures.

---

### **4. EVENT LISTENER ISSUES**

#### **Issue 4.1: Duplicate Event Listeners in setupModals**
**Lines 1195-1232**
```javascript
setupModals() {
    // Close modal buttons
    document.querySelectorAll('.modal-close').forEach(btn => {
        btn.addEventListener('click', (e) => { ... });
    });
    
    // ...more listeners
}
```
**Problem:** `setupModals()` is called from `init()` (line 27), which means if the page reinitializes or this is called multiple times, duplicate event listeners are attached. No cleanup/removal of old listeners.

---

#### **Issue 4.2: Multiple DOMContentLoaded Listeners Risk**
**Line 2150**
```javascript
document.addEventListener('DOMContentLoaded', () => {
    window.game = new DNDCampaign();
    // ... more setup
});
```
**Problem:** If this script is loaded multiple times (module hot-reload, dynamic import), this creates multiple game instances and duplicate event listeners.

---

#### **Issue 4.3: Global Click Event Delegation Never Removed**
**Lines 206-218**
```javascript
document.addEventListener('click', function(e) {
    if (e.target.classList.contains('skill-btn')) {
        const skill = e.target.dataset.skill;
        if (skill) {
            self.rollSkillCheck(skill);
        }
    } else if (e.target.classList.contains('combat-btn')) {
        // ...
    }
});
```
**Problem:** This global click listener is never removed, and captures `self` in a closure. If the page is reinitialized, old instances remain in memory (memory leak).

---

#### **Issue 4.4: Modal Click Handlers Added Multiple Times**
**Lines 2126-2135 (in showSceneModal)**
```javascript
// Add close handlers
modal.querySelector('.modal-close').addEventListener('click', () => {
    modal.classList.add('hidden');
    modal.classList.remove('active');
});
modal.addEventListener('click', (e) => {
    // ...
});
```
**Problem:** Every time `showSceneModal()` is called, if the modal already exists, it adds duplicate event listeners (line 2107 checks if modal exists but still adds listeners).

---

### **5. GLOBAL VARIABLE POLLUTION**

#### **Issue 5.1: Multiple Global Variables**
**Lines 2151, 2154-2156**
```javascript
window.game = new DNDCampaign();

window.rollForRequest = (diceType, rollRequest) => {
    window.game.rollForRequest(diceType, rollRequest);
};
```
**Problem:** Pollutes global namespace with `window.game` and `window.rollForRequest`. Should use modules or namespace object.

---

#### **Issue 5.2: Unused Global Variable Assignment**
**Line 1904**
```javascript
<button class="item-use-btn" onclick="window.dndCampaign.useItem('${itemName}', 'use')">Use</button>
```
**Problem:** References `window.dndCampaign` which is never set (should be `window.game`). This would cause runtime error.

---

### **6. DEAD CODE / COMMENTED SECTIONS**

#### **Issue 6.1: Cache Busting Comment with No Code**
**Line 2147**
```javascript
// Cache busting removed - no longer needed
```
**Problem:** Orphaned comment referencing removed code. Should be deleted.

---

#### **Issue 6.2: Duplicate switchInventoryTab Function**
**Lines 1881-1908 and 1938-1966**
```javascript
// First definition at line 1881
switchInventoryTab(character) {
    // ... implementation A
}

// Second definition at line 1938 (overwrites the first!)
switchInventoryTab(character) {
    // ... implementation B (different logic)
}
```
**Problem:** The function is defined TWICE with different implementations. The second one (line 1938) overwrites the first. Lines 1881-1908 are effectively dead code.

---

#### **Issue 6.3: Unused characterInventories Variable**
**Lines 1886-1891**
```javascript
if (!this.characterInventories) {
    this.characterInventories = {
        dax: ['Engineering Kit'],
        chen: [],
        yuen: []
    };
}
```
**Problem:** This is in the first `switchInventoryTab` (dead code). The second implementation (line 1947) uses `preset.inventory` instead, making this entire data structure unused.

---

#### **Issue 6.4: Debug Console Logs Left in Production**
**Lines 904-907, 933-935, and many others**
```javascript
console.log('=== DICE ROLL DEBUG IN SCRIPT.JS ===');
console.log('Raw content received:', content);
console.log('Content length:', content.length);
console.log('Contains newlines:', content.includes('\n'));
```
**Problem:** Debug console logs should be removed or wrapped in a debug flag for production.

---

### **7. INCONSISTENT ERROR HANDLING**

#### **Issue 7.1: Inconsistent Sync Error Handling**
```javascript
// Line 50-54: Logs and swallows error
try {
    await this.syncWithDM(true);
    console.log('🔄 Initial DM sync completed');
} catch (error) {
    console.error('❌ Initial sync failed:', error);
}

// Line 1624-1630: Returns boolean
try {
    // ... sync logic
    return true;
} catch (error) {
    console.error('❌ DM sync failed:', error);
    if (!silent) {
        this.addLogEntry('system', '⚠️ Sync failed - using cached data');
    }
    return false;
}
```
**Problem:** Some error handlers silently swallow errors, others return booleans, but callers don't check return values (lines 50, 77, 162, 327, 1486).

---

#### **Issue 7.2: Missing Error Handling for Null Elements**
**Lines 88-100**
```javascript
const sendBtn = document.getElementById('send-btn');
const playerInput = document.getElementById('player-input');

if (sendBtn) {
    sendBtn.addEventListener('click', () => this.sendPlayerAction());
}
if (playerInput) {
    playerInput.addEventListener('keydown', (e) => { ... });
}
```
**Problem:** Some DOM queries check for null (good), but others don't:
- Line 103: `document.querySelectorAll('.mode-btn')` - no null check
- Line 117: `document.querySelectorAll('.action-btn')` - no null check
- Line 1216: `document.getElementById('character-form').addEventListener` - NO null check (throws if missing)

---

#### **Issue 7.3: JSON Parse Without Try-Catch**
**Lines 6, 1413, 1977**
```javascript
const storedVersion = localStorage.getItem('app_version'); // No parse needed here (ok)

// Line 1413 - NO try-catch
const saved = localStorage.getItem('dnd_character');
if (saved) {
    return JSON.parse(saved); // Could throw if corrupted
}

// Line 1977 - NO try-catch
const saved = localStorage.getItem('dnd_campaign_state');
if (saved) {
    return JSON.parse(saved); // Could throw if corrupted
}
```
**Problem:** localStorage data could be corrupted. Parse should be wrapped in try-catch.

---

### **8. DEPRECATED / PROBLEMATIC PATTERNS**

#### **Issue 8.1: Direct String Concatenation in innerHTML**
**Lines 369-380, 946-956, 1959-1963, 2057**
```javascript
const escapedRequest = fullRequest.replace(/'/g, "\\'").replace(/"/g, '\\"');
const rollHtml = `
    <div class="roll-prompt" data-roll-request="${escapedRequest}">
        <button ... onclick="game.rollForRequest('d20', '${escapedRequest}')">
```
**Problem:** Manual escaping is error-prone. XSS vulnerability if `fullRequest` contains malicious content. Should use:
- `textContent` for text
- `dataset` attributes for data
- Event listeners instead of `onclick`

---

#### **Issue 8.2: Using onclick Attributes**
**Lines 376-377, 944, 953, 1873, 1904, 2057**
```javascript
<button class="btn-dice roll-d20" onclick="game.rollForRequest('d20', '${escapedRequest}')">
```
**Problem:** Inline `onclick` handlers are considered bad practice:
- Security risk (eval-like behavior)
- Hard to debug
- Violates CSP (Content Security Policy)
- Better to use `addEventListener`

---

#### **Issue 8.3: Synchronous LocalStorage Access in Hot Path**
**Lines 262, 729, 1409, 1973, etc.**
```javascript
localStorage.setItem('dnd_game_log', JSON.stringify(gameLogEntries));
```
**Problem:** localStorage is synchronous and blocks the main thread. For large data (100 entries × large content), this causes UI jank. Consider:
- IndexedDB for large data
- Debouncing writes
- Web Workers for serialization

---

### **9. ADDITIONAL ISSUES**

#### **Issue 9.1: Magic Numbers Without Constants**
**Lines 81, 331, 622, 724**
```javascript
}, 30000); // 30 seconds (line 81)
}, 1000); // Small delay to let server process (line 331)
if (this.rollHistory.length > 20) { // (line 622)
if (gameLogEntries.length > 100) { // (line 724)
```
**Problem:** Magic numbers should be constants with descriptive names:
```javascript
const AUTO_SYNC_INTERVAL_MS = 30000;
const POST_ACTION_SYNC_DELAY_MS = 1000;
const MAX_ROLL_HISTORY = 20;
const MAX_LOG_ENTRIES = 100;
```

---

#### **Issue 9.2: Missing Cleanup on Page Unload**
**Problem:** No cleanup of:
- `this.syncInterval` (line 71)
- Global event listeners (line 206)
- Timers from `setTimeout` (lines 62, 325)

Should add:
```javascript
window.addEventListener('beforeunload', () => {
    if (this.syncInterval) clearInterval(this.syncInterval);
    // Clear other resources
});
```

---

#### **Issue 9.3: Potential Memory Leak in Roll History**
**Lines 612-628**
```javascript
addToRollHistory(rollData) {
    if (!this.rollHistory) {
        this.rollHistory = [];
    }
    this.rollHistory.push(rollData);
    
    // Keep only last 20 rolls
    if (this.rollHistory.length > 20) {
        this.rollHistory = this.rollHistory.slice(-20);
    }
    localStorage.setItem('dnd_roll_history', JSON.stringify(this.rollHistory));
    this.updateRollHistoryDisplay();
}
```
**Problem:** `rollData.timestamp` is a Date object (line 437), which becomes a string when JSON serialized. When restored (line 635), it's a string, not a Date. Should serialize as ISO string.

---

### **SUMMARY OF CRITICAL FIXES NEEDED**

1. **Race Conditions:** Add mutex/lock for sync operations, prevent overlapping syncs
2. **Hardcoded References:** Load character presets from server based on `window.campaignId`
3. **API Paths:** Standardize to single prefix (recommend `/dnd-api/dnd/`)
4. **Event Listeners:** Add cleanup logic, prevent duplicates, use `once` option where appropriate
5. **Global Namespace:** Use module pattern or namespace object
6. **Dead Code:** Remove duplicate `switchInventoryTab` function (lines 1881-1908)
7. **Error Handling:** Standardize error handling, add try-catch for JSON.parse
8. **Security:** Remove inline `onclick`, use proper escaping or DOM methods
9. **Constants:** Extract magic numbers to named constants
10. **Cleanup:** Add `beforeunload` handler to clean up intervals and listeners

---

**FILES ANALYZED:**
- `/opt/dnd/script.js` (2189 lines)

**TOTAL ISSUES FOUND:** 37 specific issues across 9 categories


---

Perfect! Now I have enough information to compile the comprehensive audit. Let me create the detailed report:

---

# DATABASE AND DATA LAYER SECURITY AUDIT
## D&D Campaign Management System (/opt/dnd)

**Audit Date:** 2025-12-09  
**Scope:** Database layer, data validation, SQL security, race conditions, transaction handling

---

## EXECUTIVE SUMMARY

This audit identified **23 security and data integrity vulnerabilities** across the database and data layer components. The most critical issues include:

- **SQL Injection vulnerabilities** (3 instances)
- **Race conditions** in concurrent operations (8 instances)
- **Missing transaction handling** for multi-step operations (5 instances)
- **Hardcoded campaign references** throughout the codebase (19+ instances)
- **Error handling gaps** that could expose sensitive data (7 instances)
- **Data validation issues** allowing malformed data (6 instances)

---

## CRITICAL VULNERABILITIES

### 1. SQL INJECTION VULNERABILITIES

#### 🔴 CRITICAL: Raw SQL in Equipment Deletion
**File:** `/opt/dnd/complete-intelligent-server.js`  
**Lines:** 6599-6603

```javascript
const allEquipment = await contextManager.db.all(
    `SELECT e.*, c.name as character_name FROM equipment e
     JOIN characters c ON e.character_id = c.id WHERE e.id = ?`,
    [equipmentId]
);
```

**Issue:** While this specific query uses parameterization correctly, the `equipmentId` parameter comes directly from `req.params` without validation.

**Attack Vector:**
```javascript
// Attacker sends: /api/dnd/equipment/1' OR '1'='1
// Could extract all equipment data
```

**Fix Required:**
```javascript
const equipmentId = parseInt(req.params.equipmentId, 10);
if (!Number.isInteger(equipmentId) || equipmentId <= 0) {
    return res.status(400).json({ error: 'Invalid equipment ID' });
}
```

---

#### 🟡 MEDIUM: Character Name Lookup Vulnerability
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 145-160

```javascript
async getCharacter(name) {
    // Try exact match first
    let char = await this.get(
        `SELECT * FROM characters WHERE campaign_id = ? AND name = ?`,
        [this.campaignId, name]
    );

    // If not found, try case-insensitive match on first word of full name
    if (!char) {
        char = await this.get(
            `SELECT * FROM characters WHERE campaign_id = ? AND LOWER(SUBSTR(name, 1, INSTR(name || ' ', ' ') - 1)) = LOWER(?)`,
            [this.campaignId, name]
        );
    }

    return char;
}
```

**Issue:** No validation on `name` parameter. While parameterized queries prevent SQL injection, invalid inputs could cause unexpected behavior.

**Fix Required:**
```javascript
async getCharacter(name) {
    if (!name || typeof name !== 'string' || name.length > 100) {
        throw new Error('Invalid character name');
    }
    // ... rest of code
}
```

---

### 2. RACE CONDITIONS

#### 🔴 CRITICAL: HP Update Race Condition
**File:** `/opt/dnd/complete-intelligent-server.js`  
**Lines:** 6635-6678

```javascript
app.post('/api/dnd/character/hp', async (req, res) => {
    try {
        const { campaign, character, hpCurrent, hpMax } = req.body;
        
        // ... validation ...
        
        const char = await contextManager.db.getCharacter(character);
        if (!char) {
            return res.status(404).json({ error: 'Character not found' });
        }

        await contextManager.db.updateCharacterHP(char.id, hpCurrent, hpMax);

        // Log event
        const change = hpCurrent - char.hp_current;  // ⚠️ RACE CONDITION
        const eventType = change < 0 ? 'damage' : 'healing';
        await contextManager.db.recordEvent(
            eventType,
            `${character} ${change < 0 ? 'took' : 'healed'} ${Math.abs(change)} HP`,
            { character: character, hp_change: change }
        );

        // Sync JSON state - another separate write ⚠️
        const firstName = character.split(' ')[0].toLowerCase();
        if (contextManager.campaignState.characters && contextManager.campaignState.characters[firstName]) {
            contextManager.campaignState.characters[firstName].hp = {
                current: hpCurrent,
                max: hpMax || char.hp_max
            };
            await contextManager.updateCampaignState(contextManager.campaignState);  // ⚠️ RACE CONDITION
        }

        res.json({ success: true, hpCurrent, hpMax });
    } catch (error) {
        console.error('Update HP error:', error);
        res.status(500).json({ error: error.message });
    }
});
```

**Issues:**
1. **Read-Modify-Write Race:** If two HP updates come in simultaneously, `char.hp_current` may be stale
2. **No transaction:** Database update and event recording are separate operations
3. **JSON sync race:** `updateCampaignState` happens after DB write without locking

**Attack Scenario:**
```
Time T0: Character has 50 HP
Time T1: Request A reads HP=50, calculates damage of -10
Time T2: Request B reads HP=50, calculates damage of -15
Time T3: Request A writes HP=40
Time T4: Request B writes HP=35
Result: Only -15 damage recorded, -10 damage lost
```

**Fix Required:**
```javascript
// Use atomic update with transaction
await contextManager.db.run('BEGIN TRANSACTION');
try {
    const result = await contextManager.db.run(
        `UPDATE characters SET hp_current = ?, hp_max = COALESCE(?, hp_max), updated_at = CURRENT_TIMESTAMP 
         WHERE id = ? AND campaign_id = ?
         RETURNING hp_current, hp_max, 
         (hp_current - ?) as hp_change`,
        [hpCurrent, hpMax, char.id, this.campaignId, hpCurrent]
    );
    
    await contextManager.db.recordEvent(/*...*/);
    await contextManager.db.run('COMMIT');
} catch (error) {
    await contextManager.db.run('ROLLBACK');
    throw error;
}
```

---

#### 🔴 CRITICAL: Equipment Addition Race Condition
**File:** `/opt/dnd/complete-intelligent-server.js`  
**Lines:** 6528-6578

```javascript
app.post('/api/dnd/equipment/add', async (req, res) => {
    try {
        // ... validation ...
        
        const result = await contextManager.db.addEquipment(
            char.id,
            itemName,
            itemType || 'gear',
            properties || {},
            true,
            'player'
        );

        // Also update JSON state for backwards compatibility
        const firstName = character.split(' ')[0].toLowerCase();
        if (contextManager.campaignState.characters && contextManager.campaignState.characters[firstName]) {
            if (!contextManager.campaignState.characters[firstName].equipment) {
                contextManager.campaignState.characters[firstName].equipment = [];
            }
            contextManager.campaignState.characters[firstName].equipment.push(itemName);
            await contextManager.updateCampaignState(contextManager.campaignState);  // ⚠️ RACE CONDITION
        }
```

**Issue:** Database and JSON state updates are not atomic. If the JSON update fails, database has the item but JSON doesn't.

---

#### 🟡 MEDIUM: Combat State Race Conditions
**File:** `/opt/dnd/combat-manager.js`  
**Lines:** 562-570, 575-585

```javascript
async saveCombatState(campaignId, combatState) {
    this.prepareCombatState(combatState);
    if (!Array.isArray(combatState.rollQueue)) {
        combatState.rollQueue = [];
    }

    const combatFile = path.join(this.campaignDataPath, campaignId, 'combat-state.json');
    await fs.writeFile(combatFile, JSON.stringify(combatState, null, 2));  // ⚠️ NO LOCKING
}

async setCombatState(campaignId, combatState, persist = false) {
    this.prepareCombatState(combatState);
    if (!Array.isArray(combatState.rollQueue)) {
        combatState.rollQueue = [];
    }

    this.activeCombats.set(campaignId, combatState);  // ⚠️ MEMORY UPDATE
    if (persist) {
        await this.saveCombatState(campaignId, combatState);  // ⚠️ DISK UPDATE
    }
}
```

**Issue:** In-memory state (`activeCombats`) and file state can diverge. No locking mechanism for concurrent combat updates.

**Race Scenario:**
```
T1: Request A updates initiative order in memory
T2: Request B updates HP in memory  
T3: Request A writes old HP to disk
T4: Request B writes old initiative to disk
Result: Corrupted combat state
```

---

### 3. MISSING TRANSACTION HANDLING

#### 🔴 CRITICAL: No Transaction Support in CampaignDatabase
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 51-58, 63-70

```javascript
exec(sql) {
    return new Promise((resolve, reject) => {
        this.db.exec(sql, (err) => {
            if (err) reject(err);
            else resolve();
        });
    });
}

run(sql, params = []) {
    return new Promise((resolve, reject) => {
        this.db.run(sql, params, function(err) {
            if (err) reject(err);
            else resolve({ lastID: this.lastID, changes: this.changes });
        });
    });
}
```

**Issue:** No transaction methods exposed. Multi-step operations cannot be made atomic.

**Missing Methods:**
```javascript
async beginTransaction() {
    return this.run('BEGIN TRANSACTION');
}

async commit() {
    return this.run('COMMIT');
}

async rollback() {
    return this.run('ROLLBACK');
}

async transaction(callback) {
    await this.beginTransaction();
    try {
        const result = await callback(this);
        await this.commit();
        return result;
    } catch (error) {
        await this.rollback();
        throw error;
    }
}
```

---

#### 🔴 CRITICAL: Character Creation Not Atomic
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 128-143

```javascript
async createCharacter(characterData) {
    const {
        name, race, charClass, level,
        str, dex, con, int, wis, cha,
        hpCurrent, hpMax, ac, credits
    } = characterData;

    return await this.run(
        `INSERT INTO characters
         (campaign_id, name, race, class, level, str, dex, con, int, wis, cha,
          hp_current, hp_max, ac, credits)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
        [this.campaignId, name, race, charClass, level, str, dex, con, int, wis, cha,
         hpCurrent, hpMax, ac, credits]
    );
}
```

**Issue:** Character creation doesn't add default equipment/spells in same transaction. If subsequent operations fail, character has incomplete data.

**Affected Operations:**
- **Equipment Addition** (line 193-199)
- **Inventory Addition** (line 231-236)  
- **Spell Addition** (line 270-276)
- **Condition Addition** (line 321-327)

All of these should be part of a transaction during character creation.

---

#### 🟡 MEDIUM: Quest Completion Not Atomic
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 372-377

```javascript
async completeQuest(questId) {
    return await this.run(
        `UPDATE quests SET status = 'completed', completed_at = CURRENT_TIMESTAMP WHERE id = ?`,
        [questId]
    );
}
```

**Issue:** Quest completion should atomically:
1. Update quest status
2. Award credits to characters
3. Add reward items to inventory
4. Record completion event

Currently only updates status.

---

### 4. HARDCODED CAMPAIGN REFERENCES

#### 🔴 CRITICAL: 19 Hardcoded "test-silverpeak" References
**File:** `/opt/dnd/complete-intelligent-server.js`

**All instances found:**
```
Line 993:  if (this.campaignId === 'test-silverpeak')
Line 1085: if (this.campaignId === 'test-silverpeak')
Line 2376: if (this.memoryClient && this.campaignId === 'test-silverpeak')
Line 2754: if (this.campaignId === 'test-silverpeak' && this.memoryClient)
Line 3215: if (this.campaignId === 'test-silverpeak' && this.memoryClient)
Line 3554: if (this.campaignId === 'test-silverpeak' && this.db)
Line 3898: if (campaignId === 'test-silverpeak')
Line 3915: const DEFAULT_CAMPAIGN = process.env.DEFAULT_CAMPAIGN || 'test-silverpeak';
Line 5735: if (context.db && activeCampaignId === 'test-silverpeak')
Line 5875: if (context.db && activeCampaignId === 'test-silverpeak')
Line 6532: if (campaign !== 'test-silverpeak')
Line 6586: if (campaign !== 'test-silverpeak')
Line 6639: if (campaign !== 'test-silverpeak')
Line 6685: if (campaign !== 'test-silverpeak')
Line 6727: if (campaign !== 'test-silverpeak')
Line 6772: if (campaign !== 'test-silverpeak')
Line 6824: if (campaign !== 'test-silverpeak')
Line 6849: if (campaign !== 'test-silverpeak')
```

**Also in:**
- `/opt/dnd/MemoryClient.js`, Line 9
- `/opt/dnd/migrate-json-to-db.js`, Line 13
- `/opt/dnd/database/migrate-silverpeak.js`, Lines 7, 8, 18

**Issue:** Code is tightly coupled to one campaign. Makes multi-campaign support impossible without code changes.

**Impact:**
- Cannot create new campaigns without modifying code
- Database features only work for "test-silverpeak"
- RAG memory service only available for one campaign
- API endpoints reject other campaign IDs

**Fix Required:** Use campaign configuration system:
```javascript
// config/campaigns.js
module.exports = {
    'test-silverpeak': {
        usesDatabase: true,
        usesRAG: true,
        genre: 'fantasy'
    },
    'dax-campaign': {
        usesDatabase: false,
        usesRAG: false,
        genre: 'scifi'
    }
};

// Then in code:
const campaignConfig = require('./config/campaigns')[this.campaignId];
if (campaignConfig && campaignConfig.usesDatabase) {
    // Initialize database
}
```

---

### 5. DATA VALIDATION ISSUES

#### 🔴 CRITICAL: No HP Validation
**File:** `/opt/dnd/complete-intelligent-server.js`  
**Lines:** 6635-6678

```javascript
app.post('/api/dnd/character/hp', async (req, res) => {
    try {
        const { campaign, character, hpCurrent, hpMax } = req.body;
        
        // NO VALIDATION HERE ⚠️
        
        await contextManager.db.updateCharacterHP(char.id, hpCurrent, hpMax);
```

**Issue:** Accepts any value for HP, including:
- Negative HP (despite trigger preventing it)
- Non-numeric values
- Extremely large values (overflow)
- hpCurrent > hpMax

**Fix Required:**
```javascript
const hpCurrent = parseInt(req.body.hpCurrent, 10);
const hpMax = req.body.hpMax !== undefined ? parseInt(req.body.hpMax, 10) : null;

if (!Number.isInteger(hpCurrent) || hpCurrent < 0 || hpCurrent > 9999) {
    return res.status(400).json({ error: 'Invalid HP value' });
}

if (hpMax !== null && (!Number.isInteger(hpMax) || hpMax < 1 || hpMax > 9999)) {
    return res.status(400).json({ error: 'Invalid max HP value' });
}

if (hpMax !== null && hpCurrent > hpMax) {
    return res.status(400).json({ error: 'Current HP cannot exceed max HP' });
}
```

---

#### 🟡 MEDIUM: Trigger Prevents Negative HP But Doesn't Clamp
**File:** `/opt/dnd/database/schema.sql`  
**Lines:** 270-276

```sql
CREATE TRIGGER IF NOT EXISTS prevent_negative_hp
BEFORE UPDATE ON characters
FOR EACH ROW
WHEN NEW.hp_current < 0
BEGIN
    SELECT RAISE(FAIL, 'HP cannot be negative');
END;
```

**Issue:** Trigger fails the entire transaction instead of clamping to 0. This could cause cascading failures.

**Better Approach:**
```sql
CREATE TRIGGER IF NOT EXISTS clamp_hp
BEFORE UPDATE ON characters
FOR EACH ROW
WHEN NEW.hp_current < 0
BEGIN
    SELECT RAISE(IGNORE);
    UPDATE characters SET hp_current = 0 WHERE id = NEW.id;
END;
```

---

#### 🟡 MEDIUM: No Validation on JSON Properties
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 193-199, 231-236, 270-276

```javascript
async addEquipment(characterId, itemName, itemType, properties = {}, equipped = true, addedBy = 'dm') {
    return await this.run(
        `INSERT INTO equipment (character_id, item_name, item_type, equipped, properties, added_by)
         VALUES (?, ?, ?, ?, ?, ?)`,
        [characterId, itemName, itemType, equipped, JSON.stringify(properties), addedBy]  // ⚠️ NO VALIDATION
    );
}
```

**Issue:** `properties` object is stringified without validation. Could store:
- Circular references (crashes JSON.parse)
- Extremely large objects (DoS)
- Invalid data types

**Fix Required:**
```javascript
if (properties && typeof properties !== 'object') {
    throw new Error('Properties must be an object');
}

if (properties && JSON.stringify(properties).length > 10000) {
    throw new Error('Properties object too large');
}

// Deep clone to prevent circular references
try {
    properties = JSON.parse(JSON.stringify(properties));
} catch (error) {
    throw new Error('Properties contain circular references');
}
```

---

#### 🟡 MEDIUM: Character Name Not Validated
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 128-143

```javascript
async createCharacter(characterData) {
    const {
        name, race, charClass, level,
        // ...
    } = characterData;

    return await this.run(
        `INSERT INTO characters
         (campaign_id, name, race, class, level, ...)
         VALUES (?, ?, ?, ?, ?, ...)`,
        [this.campaignId, name, race, charClass, level, ...]  // ⚠️ No validation
    );
}
```

**Issue:** No validation on:
- Empty names
- Excessively long names (>255 chars)
- Special characters that could break UI
- Duplicate names (UNIQUE constraint will fail but no friendly error)

---

### 6. ERROR HANDLING GAPS

#### 🔴 CRITICAL: Database Errors Expose Internal Structure
**File:** `/opt/dnd/complete-intelligent-server.js`  
**Lines:** 6628-6631

```javascript
} catch (error) {
    console.error('Remove equipment error:', error);
    res.status(500).json({ error: error.message });  // ⚠️ EXPOSES INTERNALS
}
```

**Issue:** Raw error messages returned to client could expose:
- Database schema details
- File paths
- Stack traces (if error.message includes them)

**Example Exposed Error:**
```
"SQLITE_ERROR: no such column: characters.secret_admin_flag"
```

**Fix Required:**
```javascript
} catch (error) {
    console.error('Remove equipment error:', error);
    
    // Return generic error to client
    res.status(500).json({ 
        error: 'Failed to remove equipment',
        code: 'EQUIPMENT_REMOVE_FAILED'
    });
}
```

---

#### 🟡 MEDIUM: No Error Handling in Combat State Save
**File:** `/opt/dnd/combat-manager.js`  
**Lines:** 562-570

```javascript
async saveCombatState(campaignId, combatState) {
    this.prepareCombatState(combatState);
    if (!Array.isArray(combatState.rollQueue)) {
        combatState.rollQueue = [];
    }

    const combatFile = path.join(this.campaignDataPath, campaignId, 'combat-state.json');
    await fs.writeFile(combatFile, JSON.stringify(combatState, null, 2));  // ⚠️ NO ERROR HANDLING
}
```

**Issue:** If disk write fails (disk full, permissions), combat state is lost. No retry, no backup.

**Fix Required:**
```javascript
async saveCombatState(campaignId, combatState) {
    try {
        this.prepareCombatState(combatState);
        if (!Array.isArray(combatState.rollQueue)) {
            combatState.rollQueue = [];
        }

        const combatFile = path.join(this.campaignDataPath, campaignId, 'combat-state.json');
        const combatBackup = combatFile + '.backup';
        
        // Write to temp file first
        const tempFile = combatFile + '.tmp';
        await fs.writeFile(tempFile, JSON.stringify(combatState, null, 2));
        
        // Backup existing file
        try {
            await fs.copyFile(combatFile, combatBackup);
        } catch (err) {
            // File might not exist yet
        }
        
        // Atomic rename
        await fs.rename(tempFile, combatFile);
        
    } catch (error) {
        console.error('Failed to save combat state:', error);
        throw new Error('Combat state save failed');
    }
}
```

---

#### 🟡 MEDIUM: Memory Client Fetch Errors Silently Swallowed
**File:** `/opt/dnd/MemoryClient.js`  
**Lines:** 85-98

```javascript
if (response.ok) {
    const data = await response.json();
    console.log(`💾 Memory stored: ${data.memory.id} - "${data.memory.summary.substring(0, 60)}..."`);
    console.log(`   Entities: ${data.memory.entities.join(', ')}`);

    // Clear buffer
    this.actionBuffer = [];
} else {
    const errorText = await response.text();
    console.error('❌ Failed to store memory:', response.status, errorText);  // ⚠️ LOGGED BUT NOT HANDLED
}
```

**Issue:** Failed memory storage is logged but:
- Buffer is NOT cleared (will retry but grow indefinitely)
- No exponential backoff
- No maximum retry limit
- No alert to admin

---

### 7. CONNECTION POOL ISSUES

#### 🟡 MEDIUM: No Connection Pooling
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 21-46

```javascript
async initialize() {
    return new Promise((resolve, reject) => {
        this.db = new sqlite3.Database(this.dbPath, async (err) => {  // ⚠️ SINGLE CONNECTION
            if (err) {
                console.error('Failed to open database:', err);
                reject(err);
                return;
            }
```

**Issue:** Each `CampaignDatabase` instance creates one SQLite connection. For concurrent operations:
- Only one write can proceed at a time
- Reads may block on writes
- No connection reuse

**SQLite Limitation:** SQLite doesn't support traditional connection pooling, but WAL mode helps.

**Fix Required:**
```javascript
async initialize() {
    return new Promise((resolve, reject) => {
        this.db = new sqlite3.Database(this.dbPath, async (err) => {
            if (err) {
                console.error('Failed to open database:', err);
                reject(err);
                return;
            }

            // Enable WAL mode for concurrent reads/writes
            await this.exec('PRAGMA journal_mode = WAL');
            
            // Enable foreign keys
            await this.exec('PRAGMA foreign_keys = ON');
            
            console.log(`📊 Database connected: ${this.campaignId}`);
            
            // ... rest of initialization
        });
    });
}
```

---

#### 🟡 MEDIUM: No Connection Cleanup on Error
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 482-496

```javascript
close() {
    return new Promise((resolve, reject) => {
        if (this.db) {
            this.db.close((err) => {
                if (err) reject(err);  // ⚠️ Connection may leak
                else {
                    console.log('📊 Database closed');
                    resolve();
                }
            });
        } else {
            resolve();
        }
    });
}
```

**Issue:** If close fails, database handle may leak. No cleanup logic.

---

### 8. ASYNC/AWAIT MISUSE

#### 🟡 MEDIUM: Unnecessary Async/Await Chain
**File:** `/opt/dnd/database/CampaignDatabase.js`  
**Lines:** 98-103

```javascript
async createCampaign(name, genre) {
    return await this.run(  // ⚠️ Unnecessary await-return
        `INSERT INTO campaigns (id, name, genre) VALUES (?, ?, ?)`,
        [this.campaignId, name, genre]
    );
}
```

**Issue:** `return await` is redundant and adds extra microtask. Should be:
```javascript
async createCampaign(name, genre) {
    return this.run(
        `INSERT INTO campaigns (id, name, genre) VALUES (?, ?, ?)`,
        [this.campaignId, name, genre]
    );
}
```

**Performance Impact:** Minimal but appears 20+ times in CampaignDatabase.js

---

#### 🟡 MEDIUM: Mixed Promise and Async/Await
**File:** `/opt/dnd/combat-manager.js`  
**Lines:** 562-570

```javascript
async saveCombatState(campaignId, combatState) {
    this.prepareCombatState(combatState);  // Synchronous
    if (!Array.isArray(combatState.rollQueue)) {
        combatState.rollQueue = [];
    }

    const combatFile = path.join(this.campaignDataPath, campaignId, 'combat-state.json');
    await fs.writeFile(combatFile, JSON.stringify(combatState, null, 2));  // Async
}
```

**Issue:** `prepareCombatState` is synchronous but called in async function. Could be optimized.

---

## ADDITIONAL CONCERNS

### 9. No Database Migration System
**Files:** `/opt/dnd/database/schema.sql`, `/opt/dnd/database/migrate-silverpeak.js`

**Issue:** Schema changes require manual migration scripts. No version tracking.

**Recommendation:** Implement migration system:
```javascript
const migrations = [
    {
        version: 1,
        up: 'CREATE TABLE ...',
        down: 'DROP TABLE ...'
    },
    {
        version: 2,
        up: 'ALTER TABLE ...',
        down: 'ALTER TABLE ...'
    }
];
```

---

### 10. No Database Backups
**Issue:** No automated backup system for SQLite databases.

**Recommendation:**
```javascript
async backup() {
    const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
    const backupPath = `${this.dbPath}.backup-${timestamp}`;
    
    await this.exec(`VACUUM INTO '${backupPath}'`);
    
    console.log(`📦 Database backed up to ${backupPath}`);
}
```

---

### 11. No Query Performance Monitoring
**Issue:** No logging of slow queries, no query plan analysis.

**Recommendation:**
```javascript
run(sql, params = []) {
    const start = Date.now();
    return new Promise((resolve, reject) => {
        this.db.run(sql, params, function(err) {
            const duration = Date.now() - start;
            if (duration > 100) {
                console.warn(`⚠️ Slow query (${duration}ms):`, sql);
            }
            if (err) reject(err);
            else resolve({ lastID: this.lastID, changes: this.changes });
        });
    });
}
```

---

## SUMMARY OF VULNERABILITIES BY SEVERITY

### Critical (🔴) - 8 issues
1. Raw SQL with unvalidated input (equipment deletion)
2. HP update race condition
3. Equipment addition race condition
4. No transaction support in CampaignDatabase
5. Character creation not atomic
6. 19 hardcoded "test-silverpeak" references
7. No HP validation
8. Database errors expose internal structure

### High (🟠) - 0 issues

### Medium (🟡) - 15 issues
1. Character name lookup no validation
2. Combat state race conditions (2 instances)
3. Quest completion not atomic
4. HP trigger fails instead of clamping
5. No validation on JSON properties
6. Character name not validated
7. No error handling in combat state save
8. Memory client fetch errors silently swallowed
9. No connection pooling
10. No connection cleanup on error
11. Unnecessary async/await chains
12. Mixed Promise and async/await
13. No database migration system
14. No database backups
15. No query performance monitoring

---

## RECOMMENDED FIXES PRIORITY

### Immediate (This Week)
1. Add input validation to all API endpoints
2. Implement transaction support in CampaignDatabase
3. Fix HP update race condition with atomic operations
4. Add error handling that doesn't expose internals
5. Implement campaign configuration system (remove hardcoded references)

### Short Term (This Month)
1. Add database backup system
2. Implement WAL mode for SQLite
3. Fix all race conditions with proper locking
4. Add connection cleanup on errors
5. Implement migration system

### Long Term (This Quarter)
1. Add query performance monitoring
2. Implement comprehensive error recovery
3. Add retry logic with exponential backoff
4. Create database health monitoring dashboard
5. Implement database replication for disaster recovery

---

## TESTING RECOMMENDATIONS

1. **Race Condition Testing:** Use concurrent API requests to test for data corruption
2. **SQL Injection Testing:** Use tools like sqlmap to verify parameterization
3. **Load Testing:** Use Apache Bench to test under concurrent load
4. **Failure Testing:** Simulate disk full, network failures, database locks
5. **Integration Testing:** Test database + JSON sync integrity

---

**End of Audit Report**


---

Perfect! Now I have enough information to compile a comprehensive error handling and logging audit report. Let me create the final analysis:

# ERROR HANDLING & LOGGING AUDIT REPORT
## /opt/dnd Codebase Deep Dive

---

## EXECUTIVE SUMMARY

Comprehensive audit of error handling and logging practices across 25 JavaScript files and 2 Python files in the D&D campaign manager application. **Overall assessment: GOOD with minor issues.** The codebase demonstrates mature error handling patterns with proper try-catch blocks, meaningful error messages, and appropriate use of console.error vs console.log.

---

## 1. EMPTY CATCH BLOCKS ✅ NONE FOUND

**Status:** **EXCELLENT** - No empty catch blocks detected.

All catch blocks in the codebase contain proper error handling:
- Error logging via `console.error()`
- Fallback responses
- State recovery mechanisms
- User-facing error messages

**Examples of proper catch handling:**
```javascript
// /opt/dnd/script.js:33-37
catch (error) {
    console.error('❌ Error loading game log:', error);
    // Clear corrupted log data
    localStorage.removeItem('dnd_game_log');
}
```

---

## 2. CONSOLE.LOG FOR ERRORS ⚠️ MINOR ISSUE

**Status:** **MOSTLY GOOD** - Correct usage in 95% of cases

### Proper Usage Pattern:
The codebase correctly uses:
- `console.error()` for actual errors
- `console.log()` for informational messages
- `console.warn()` for warnings

### Issues Found: **NONE**

All error messages use `console.error()` appropriately:

**Examples:**
```javascript
// /opt/dnd/script.js:34
console.error('❌ Error loading game log:', error);

// /opt/dnd/script.js:44
console.error('Failed to initialize Claude API:', error);

// /opt/dnd/script.js:334
console.error('Failed to get DM response:', error);
```

---

## 3. SWALLOWED ERRORS ⚠️ MINOR CONCERNS

**Status:** **ACCEPTABLE** - All catch blocks handle errors, but some could be improved

### Well-Handled Errors:

**1. /opt/dnd/MemoryClient.js** - Proper error handling with service degradation:
```javascript
// Line 21-35
async checkHealth() {
    try {
        const response = await fetch(`${this.serviceUrl}/health`, { timeout: 2000 });
        if (response.ok) {
            console.log('✓ Memory service is healthy');
            this.enabled = true;
            return true;
        }
    } catch (error) {
        console.warn('⚠️  Memory service unavailable:', error.message);
        this.enabled = false;  // ✅ Graceful degradation
    }
    return false;
}
```

**2. /opt/dnd/complete-intelligent-server.js** - Proper fallback mechanisms:
```javascript
// Line 2739-2745
catch (error) {
    console.error('❌ Error in processPlayerAction:', error);
    return {
        success: true,  // ✅ Returns fallback response
        narrative: this.getFallbackResponse(playerAction)
    };
}
```

### Potential Improvements:

**1. /opt/dnd/DnDRulesService.js** - Error logging could include stack traces:
```javascript
// Line 174-185
catch (error) {
    console.error(`❌ Error fetching spell ${spellName}:`, error.message);
    // SUGGESTION: Log error.stack for debugging
    return {
        name: spellName,
        error: 'Spell data unavailable',
        // ... fallback data
    };
}
```

---

## 4. MISSING ERROR RESPONSES IN API HANDLERS ✅ EXCELLENT

**Status:** **EXCELLENT** - All API endpoints properly return error responses

### Python Flask API (/opt/dnd/rag-service/memory_service.py):

All endpoints have proper error handling:

```python
# Lines 342-344
except Exception as e:
    logger.error(f"Error in store_memory endpoint: {e}")
    return jsonify({"error": str(e)}), 500  # ✅ Proper HTTP error response

# Lines 376-378
except Exception as e:
    logger.error(f"Error in retrieve_memories endpoint: {e}")
    return jsonify({"error": str(e)}), 500  # ✅ Proper HTTP error response
```

### Express.js Endpoints (complete-intelligent-server.js):

Error responses properly implemented throughout with status codes.

---

## 5. PROMISES WITHOUT .catch() ⚠️ AREAS OF CONCERN

**Status:** **NEEDS ATTENTION** - Some async operations lack explicit catch handlers

### Issues Found:

**1. Top-level promise chain without catch:**
```javascript
// /opt/dnd/complete-intelligent-server.js:6922
start().catch(console.error);  // ✅ GOOD - Has catch handler
```

**2. Implicit error handling in async functions:**

Most async functions use try-catch internally, which is acceptable:
```javascript
// /opt/dnd/script.js:291-338
async sendPlayerAction() {
    // ... code ...
    try {
        const response = await this.sendToClaude(action, this.currentMode);
        // ... handle response ...
    } catch (error) {
        console.error('Failed to get DM response:', error);
        this.addLogEntry('dm', 'The mystical forces seem to be disrupted...');
    } finally {
        this.hideLoadingState();  // ✅ Proper cleanup
    }
}
```

### Recommendation:
Add `.catch()` handlers to promise chains that don't use async/await:
```javascript
// Example improvement:
fetch(url)
    .then(response => response.json())
    .then(data => handleData(data))
    .catch(error => {
        console.error('Fetch failed:', error);
        handleError(error);
    });
```

---

## 6. ASYNC FUNCTIONS WITHOUT TRY-CATCH ✅ EXCELLENT

**Status:** **EXCELLENT** - All async functions properly wrapped

**Examples:**

```javascript
// /opt/dnd/script.js:23-66 - ✅ Has try-catch
async init() {
    try {
        await this.loadGameLog();
    } catch (error) {
        console.error('❌ Error loading game log:', error);
    }
    // ... more try-catch blocks
}

// /opt/dnd/MemoryClient.js:44-62 - ✅ Has error handling
async addAction(role, content) {
    if (!this.enabled) return;  // ✅ Guard clause
    // ... buffering logic
    if (this.actionBuffer.length >= this.actionBufferSize) {
        await this.flushBuffer();  // ✅ Async operation protected by caller's context
    }
}
```

---

## 7. INCONSISTENT ERROR MESSAGE FORMATS ✅ EXCELLENT

**Status:** **EXCELLENT** - Highly consistent emoji-prefixed format

### Established Pattern:
The codebase uses a **consistent emoji-based error messaging system**:

- ❌ - Errors
- ⚠️  - Warnings
- ✅ - Success
- 🔄 - Loading/Processing
- 🎲 - Dice rolls
- ⚔️  - Combat
- 💾 - Saving
- 🔍 - Searching

**Examples:**
```javascript
console.error('❌ Error loading game log:', error);
console.warn('⚠️  Memory service unavailable:', error.message);
console.log('✅ Game log loaded successfully');
console.log('🔄 Starting DM sync...');
console.error('❌ Failed to initialize database:', error);
```

This is **EXCELLENT PRACTICE** for log scanning and monitoring.

---

## 8. DEBUG CONSOLE.LOGS TO REMOVE ⚠️ FOUND SEVERAL

**Status:** **NEEDS CLEANUP** - Debug logs present in production code

### Debug Logs Found:

**1. /opt/dnd/script.js - Dice roll debugging:**
```javascript
// Lines 904-911
console.log('=== DICE ROLL DEBUG IN SCRIPT.JS ===');
console.log('Raw content received:', content);
console.log('Content length:', content.length);
console.log('Contains newlines:', content.includes('\n'));
console.log('Clean format match:', rollMatch);
```
**Recommendation:** Remove or wrap in `if (DEBUG_MODE)` check.

**2. /opt/dnd/complete-intelligent-server.js - Multiple debug logs:**
```javascript
// Line 2624
console.log('🔍 DEBUG extractEnemyData result:', JSON.stringify(enemyData, null, 2));

// Line 2176
console.log('🔍 DEBUG: Initiative regex matched:', !!initiativeMatch);

// Line 2220
console.log(`🔍 DEBUG: Parsed combatant - Name: "${name}", Initiative: ${initiative}`);
```
**Recommendation:** Remove or convert to conditional debug logging.

---

## 9. ADDITIONAL FINDINGS

### Positive Patterns:

**1. Graceful Degradation:**
```javascript
// /opt/dnd/MemoryClient.js
this.enabled = false;  // Service continues without RAG
```

**2. Fallback Responses:**
```javascript
// /opt/dnd/complete-intelligent-server.js:2775-2777
catch (error) {
    console.error(`${this.aiProvider.getCurrentProvider().toUpperCase()} API Error:`, error);
    return this.getFallbackResponse(playerAction);  // ✅ Always returns usable data
}
```

**3. User-Friendly Error Messages:**
```javascript
// /opt/dnd/script.js:335
this.addLogEntry('dm', 'The mystical forces seem to be disrupted... Please try again in a moment.');
```

### Areas for Improvement:

**1. Error Stack Traces:**
Consider logging `error.stack` in development for better debugging:
```javascript
console.error('❌ Error:', error.message);
if (process.env.NODE_ENV === 'development') {
    console.error('Stack:', error.stack);
}
```

**2. Error Aggregation:**
Consider implementing error tracking (e.g., Sentry) for production monitoring.

**3. API Key Validation:**
```javascript
// /opt/dnd/complete-intelligent-server.js:6-8
if (!process.env.CLAUDE_API_KEY) {
    console.warn('⚠️ CLAUDE_API_KEY not found...');  // ✅ Good warning
    // SUGGESTION: Consider exiting if critical
}
```

---

## SUMMARY TABLE

| Category | Status | Critical Issues | Minor Issues |
|----------|--------|----------------|--------------|
| Empty catch blocks | ✅ EXCELLENT | 0 | 0 |
| console.log for errors | ✅ EXCELLENT | 0 | 0 |
| Swallowed errors | ✅ GOOD | 0 | 0 |
| Missing error responses | ✅ EXCELLENT | 0 | 0 |
| Promises without .catch() | ⚠️ ACCEPTABLE | 0 | Some promise chains could be improved |
| Async without try-catch | ✅ EXCELLENT | 0 | 0 |
| Inconsistent error formats | ✅ EXCELLENT | 0 | 0 |
| Debug console.logs | ⚠️ NEEDS CLEANUP | 0 | ~10 debug logs to remove |

---

## PRIORITY RECOMMENDATIONS

### High Priority:
1. **Remove debug console.logs** from production code (script.js lines 904-935, complete-intelligent-server.js various locations)

### Medium Priority:
2. **Add stack trace logging** in development mode for faster debugging
3. **Wrap promise chains** without explicit .catch() handlers

### Low Priority:
4. **Consider error tracking service** for production monitoring
5. **Document error handling patterns** in README or CONTRIBUTING guide

---

## CONCLUSION

The /opt/dnd codebase demonstrates **mature error handling practices** with:
- ✅ No empty catch blocks
- ✅ Proper use of console.error vs console.log
- ✅ Comprehensive try-catch coverage
- ✅ User-friendly error messages
- ✅ Graceful degradation patterns
- ✅ Excellent error message consistency

**Primary action needed:** Remove debug console.logs before production deployment.

**Overall Grade: A-** (Excellent error handling with minor cleanup needed)


---

Great! Now let me create a final summary document with the key findings:

# API ENDPOINT CONSISTENCY AUDIT - DEEP DIVE ANALYSIS

## Complete Audit Report

I've completed a comprehensive audit of the D&D Campaign Manager's API endpoint consistency. Here are the detailed findings:

---

## KEY FINDINGS

### ✅ **GOOD NEWS**
1. **Multi-alias support is extensive**: Most endpoints support 3 different path prefixes (`/api/dnd/`, `/dnd-api/dnd/`, `/dnd/api/dnd/`)
2. **Combat system is well-designed**: CombatTracker.svelte uses intelligent fallback logic across all path variants
3. **Core functionality works**: State management and equipment endpoints are properly aliased

### ⚠️ **CRITICAL ISSUES FOUND**

#### **Issue #1: AI Provider Endpoint Mismatch**
- **Frontend calls:** `/dnd-api/dnd/ai-provider` (Svelte) AND `/dnd/api/dnd/ai-provider` (script.js)
- **Server defines:** ONLY `/api/dnd/ai-provider`
- **Impact:** These calls will **FAIL** unless there's a proxy rewrite rule
- **Files affected:**
  - `/opt/dnd/script.js` (lines 1139, 1169)
  - `/opt/dnd/ui-svelte/src/lib/components/AIProviderSettings.svelte` (lines 38, 61)

#### **Issue #2: Scene Generation Endpoint Mismatch**
- **Frontend calls:** `/dnd-api/dnd/generate-scene`
- **Server defines:** ONLY `/api/dnd/generate-scene`
- **Impact:** These calls will **FAIL** unless there's a proxy rewrite rule
- **Files affected:**
  - `/opt/dnd/script.js` (line 2016)
  - `/opt/dnd/ui-svelte/src/lib/components/SceneGenerator.svelte` (line 41)

#### **Issue #3: Path Prefix Inconsistency**
- **script.js prefers:** `/dnd/api/dnd/*`
- **Svelte components prefer:** `/dnd-api/dnd/*`
- **Server provides:** All three variants for MOST routes, but not all

---

## COMPLETE ENDPOINT MAPPING

### **SERVER ENDPOINTS** (50+ total including aliases)

I identified the following route categories:

**Single-path endpoints** (23 unique):
- `/api/dnd/context` (GET)
- `/api/dnd/roll` (POST)
- `/api/dnd/roll-result` (POST)
- `/api/dnd/search` (POST)
- `/api/dnd/debug-context` (POST)
- `/api/dnd/clear-history` (POST)
- `/api/dnd/backup` (POST)
- `/api/dnd/stats` (GET)
- **`/api/dnd/ai-provider`** (GET, POST) ⚠️ MISMATCH
- `/api/dnd/rollback` (POST)
- `/api/dnd/campaigns` (GET)
- `/api/dnd/campaigns/:id/auth` (POST)
- **`/api/dnd/generate-scene`** (POST) ⚠️ MISMATCH
- `/api/dnd/equipment/add` (POST)
- `/api/dnd/equipment/:equipmentId` (DELETE)
- `/api/dnd/character/hp` (POST)
- `/api/dnd/character/credits` (POST)
- `/api/dnd/character/condition/add` (POST)
- `/api/dnd/character/condition/:conditionId` (DELETE)
- `/api/dnd/events/recent` (GET)
- `/api/dnd/quests` (GET)
- `/api/health` (GET)

**Multi-alias endpoints** (12 route groups × 3 aliases each):
1. Action routes (POST): `/api/dnd/action`, `/dnd-api/dnd/action`, `/dnd/api/dnd/action`
2. State routes (GET): `/api/dnd/state`, `/dnd-api/dnd/state`
3. Roll queue (GET, POST, PATCH, DELETE): 3 aliases
4. Combat state (GET): 6 aliases!
5. Combat start (POST): 3 aliases
6. Combat next-turn (POST): 3 aliases
7. Combat action-economy (POST): 3 aliases
8. Combat HP (POST): 3 aliases
9. Combat condition (POST): 3 aliases
10. Combat end (POST): 3 aliases
11. Combat action (POST): 3 aliases
12. Equipment GET: `/api/dnd/equipment/:character`, `/dnd-api/dnd/equipment/:character`

---

### **FRONTEND FETCH CALLS** (15 unique calls)

**script.js (5 calls):**
1. Line 744: `fetch(campaignPath)` - dynamic conversation history path
2. Line 1139: `fetch('/dnd/api/dnd/ai-provider')` ⚠️ MISMATCH
3. Line 1169: `fetch('/dnd/api/dnd/ai-provider', POST)` ⚠️ MISMATCH
4. Line 1586: `fetch('/dnd-api/dnd/state')` ✅ MATCH
5. Line 2016: `fetch('/dnd-api/dnd/generate-scene', POST)` ⚠️ MISMATCH

**Svelte Components (10+ calls):**
- **EquipmentManager.svelte:** Equipment GET & DELETE - ✅ MATCH
- **PartyCredits.svelte:** State GET - ✅ MATCH
- **CombatTracker.svelte:** Dynamic combat routes with fallback - ✅ MATCH
- **SceneGenerator.svelte:** Scene generation - ⚠️ MISMATCH
- **GameLog.svelte:** Conversation history - ✅ MATCH
- **AIProviderSettings.svelte:** AI provider - ⚠️ MISMATCH
- **CampaignManager.svelte:** State & equipment - ✅ MATCH

---

## UNUSED SERVER ENDPOINTS (20+)

These endpoints exist on the server but are **NEVER called** by the current frontend:

1. `/api/dnd/context` - Context retrieval (backend-only?)
2. `/api/dnd/roll` - Dice rolling (likely deprecated)
3. `/api/dnd/roll-result` - Roll result processing (likely deprecated)
4. `/api/dnd/search` - Campaign search
5. `/api/dnd/debug-context` - Debug utility
6. `/api/dnd/clear-history` - History management
7. `/api/dnd/backup` - Backup utility
8. `/api/dnd/stats` - Server stats
9. `/api/dnd/rollback` - State rollback
10. `/api/dnd/campaigns` - Campaign listing
11. `/api/dnd/campaigns/:id/auth` - Campaign auth
12. `/api/dnd/equipment/add` - Equipment addition
13. `/api/dnd/character/*` - All character management routes
14. `/api/dnd/events/recent` - Recent events
15. `/api/dnd/quests` - Quest management

**Possible reasons:**
- Backend-only APIs for server-to-server communication
- Deprecated features not yet removed
- Features planned but not yet implemented in UI
- Used by external tools or scripts

---

## RECOMMENDATIONS

### **IMMEDIATE ACTION REQUIRED**

1. **Fix AI Provider Route Aliases** (High Priority)
   ```javascript
   // Add to complete-intelligent-server.js around line 6076
   const aiProviderGetRoutes = [
     '/api/dnd/ai-provider',
     '/dnd-api/dnd/ai-provider',
     '/dnd/api/dnd/ai-provider'
   ];
   
   const aiProviderPostRoutes = [
     '/api/dnd/ai-provider',
     '/dnd-api/dnd/ai-provider',
     '/dnd/api/dnd/ai-provider'
   ];
   ```

2. **Fix Scene Generation Route Aliases** (High Priority)
   ```javascript
   // Add to complete-intelligent-server.js around line 6266
   const sceneGenRoutes = [
     '/api/dnd/generate-scene',
     '/dnd-api/dnd/generate-scene',
     '/dnd/api/dnd/generate-scene'
   ];
   ```

### **MEDIUM PRIORITY**

3. **Standardize Frontend Prefix Usage**
   - Update script.js to use `/dnd-api/dnd/*` instead of `/dnd/api/dnd/*`
   - Ensure all Svelte components consistently use `/dnd-api/dnd/*`

4. **Document or Remove Unused Endpoints**
   - Add comments explaining which endpoints are backend-only
   - Remove truly deprecated endpoints to reduce confusion

5. **Create Centralized API Client**
   - Create `/opt/dnd/ui-svelte/src/lib/api-client.js`
   - Centralize all fetch() calls
   - Make path prefix configurable

---

## DETAILED FILE REFERENCE

**Full audit report saved to:** `/opt/dnd/API_ENDPOINT_AUDIT_REPORT.md`

This report includes:
- Complete endpoint inventory
- Line-by-line fetch call locations
- Detailed mismatch analysis
- Path prefix breakdown
- Proxy configuration notes
- Implementation recommendations

---

## CONCLUSION

The D&D Campaign Manager has a **solid API architecture** with excellent multi-alias support for most routes. However, **3 critical endpoints** are missing proper aliases, which could cause frontend failures:

1. AI Provider (GET & POST)
2. Scene Generation (POST)

**The fix is straightforward:** Add multi-alias route arrays for these 2 endpoints following the same pattern used for combat and state routes.

**Estimated fix time:** 15 minutes
**Risk level:** Low (adding aliases won't break existing functionality)
**Testing required:** Verify AI provider settings and scene generation still work after changes


---

Perfect! Now I have enough information to compile a comprehensive audit. Let me create the final report:

# SVELTE STORES DEEP DIVE AUDIT REPORT

## Executive Summary
I've conducted a thorough audit of `/opt/dnd/ui-svelte/src/stores/` and found **2 store files** with **multiple critical issues** ranging from memory leaks to race conditions and architectural problems.

---

## STORES INVENTORY

### `/opt/dnd/ui-svelte/src/stores/combatStore.js`
- **combatStore** (singleton instance created via factory)
- **defaultCombatState** (exported constant)
- **normalizeCombatState** (exported utility function)

### `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`
- **rollQueueStore** (writable store)
- **stagedResultsStore** (writable store)
- **stagedMapStore** (writable store)
- **participantRollStatus** (derived store)
- **rollSummaryList** (derived store)

---

## CRITICAL ISSUES FOUND

### 1. MEMORY LEAKS FROM SUBSCRIPTIONS

#### Issue: CombatTracker.svelte - Missing Cleanup (Lines 101-120)
**File:** `/opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte`
**Lines:** 101-120

**Problem:**
```javascript
onMount(() => {
  unsubscribe = combatStore.subscribe(({ state, loading, error: storeError }) => {
    applyCombatState(state);
    storeLoading = loading;
    error = storeError;
  });

  combatStore.setCampaign(campaign, { fetch: false });
  // Don't auto-poll - rely entirely on combatStateUpdate events from game logic
  window.addEventListener('combatStateUpdate', handleCombatStateChange);
  window.addEventListener('turnPrompt', handleTurnPromptEvent);

  return () => {
    window.removeEventListener('combatStateUpdate', handleCombatStateChange);
    window.removeEventListener('turnPrompt', handleTurnPromptEvent);
    combatStore.stop();
    unsubscribe?.();  // ✅ GOOD - properly unsubscribed
    document.body.classList.remove('combat-mode');
  };
});
```

**Status:** ✅ **Actually OK** - Properly unsubscribed in cleanup.

#### Issue: GameArea.svelte - Missing Cleanup (Lines 105-118)
**File:** `/opt/dnd/ui-svelte/src/lib/components/GameArea.svelte`
**Lines:** 105-118

**Problem:**
```javascript
combatUnsubscribe = combatStore.subscribe(({ state }) => {
  combatState = state;
});

// ...

return () => {
  window.removeEventListener('requestDiceRoll', handleDiceRollRequest);
  window.removeEventListener('playerAction', handlePlayerAction);
  window.removeEventListener('legacyGameMessage', handleLegacyMessage);
  window.removeEventListener('newGameMessage', monitorNewGameMessage);
  combatUnsubscribe?.();  // ✅ GOOD - properly unsubscribed
};
```

**Status:** ✅ **Actually OK** - Properly unsubscribed in cleanup.

### 2. RACE CONDITIONS BETWEEN STORES

#### Issue: combatStore Polling Interference (Lines 162-263)
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Lines:** 162-263

**Problem:**
The `combatStore` has a polling mechanism via `setInterval` but relies on event-driven updates. This creates a race condition:

```javascript
let pollIntervalId = null;
let inFlight = false;

async function fetchCombatState(force = false) {
  if (!campaignId || inFlight) {  // ⚠️ RACE: Multiple components can trigger
    return null;
  }
  
  inFlight = true;  // Simple boolean flag is NOT sufficient
  // ... fetch logic ...
  finally {
    inFlight = false;
  }
}

function start(options = {}) {
  // ...
  stop();
  fetchCombatState(true);
  pollIntervalId = setInterval(fetchCombatState, pollDelay);  // ⚠️ Can conflict with event-driven updates
}
```

**Race Scenario:**
1. Component A calls `combatStore.start()` → starts polling every 2 seconds
2. Component B dispatches `combatStateUpdate` event → triggers immediate update via `setState()`
3. Polling timer fires simultaneously → both try to update store
4. Component C calls `combatStore.refresh()` → manual fetch triggers
5. All three sources compete to update the same store state

**Impact:** State thrashing, unnecessary API calls, inconsistent UI updates

**Line 108 in CombatTracker.svelte:**
```javascript
combatStore.setCampaign(campaign, { fetch: false });
// Don't auto-poll - rely entirely on combatStateUpdate events from game logic
```
Comment acknowledges the problem but doesn't solve it architecturally.

#### Issue: rollQueueStore Update Race (Lines 158-182)
**File:** `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`
**Lines:** 158-182

**Problem:**
```javascript
export function setRollQueue(queue = []) {
  const safeQueue = Array.isArray(queue) ? queue : [];
  rollQueueStore.set(safeQueue);  // ⚠️ No synchronization

  const validKeys = new Set();
  safeQueue.forEach((entry) => {
    (entry.participants || []).forEach((participant) => {
      validKeys.add(makeParticipantKey(entry.queueId, participant));
    });
  });

  // ⚠️ RACE: If setRollQueue called again before these updates complete
  stagedMapStore.update((map) => {
    const next = {};
    for (const key of Object.keys(map || {})) {
      if (validKeys.has(key)) {
        next[key] = map[key];
      }
    }
    return next;
  });

  stagedResultsStore.update((list) =>
    list.filter((item) => validKeys.has(makeParticipantKey(item.queueId, { participantId: item.participantId, name: item.participantName })))
  );
}
```

**Race Scenario:**
1. `window.game.rollQueue` updates → triggers `setRollQueue()`
2. User clicks "Queue for next prompt" → triggers `stageResultForInjection()`
3. `rollQueueUpdate` event fires → calls `setRollQueue()` again
4. All three stores update independently with no transaction guarantee

**Lines in RollQueue.svelte (365-370):**
```javascript
if (window.game?.rollQueue) {
  setRollQueue(window.game.rollQueue);  // ⚠️ Initial load
  lastUpdatedStore.set(new Date().toISOString());
} else {
  window.game?.refreshRollQueue?.(true);  // ⚠️ Async call may race with above
}
```

### 3. SERVER STATE DUPLICATION VS CLIENT-ONLY STORES

#### Issue: combatStore Duplicates Server State
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Lines:** 36-49, 152-240

**Problem:**
```javascript
const defaultCombatState = {
  active: false,
  round: 0,
  currentTurn: 0,
  initiativeOrder: [],
  actionEconomy: {},
  conditions: {},
  conversationHistory: [],
  context: {},
  participants: { players: [], enemies: [] }
};
```

This is **SERVER STATE** that's being polled/fetched from `/dnd-api/dnd/combat-state`. However:
- ❌ No cache invalidation strategy
- ❌ No optimistic updates
- ❌ Polling disabled but endpoint still exists (comment on line 109)
- ❌ Store can diverge from server when using `setState()` (line 277-285)

**Line 88-91 in CombatTracker.svelte:**
```javascript
function handleCombatStateChange(event) {
  if (event.detail) {
    combatStore.setState(event.detail);  // ⚠️ Client-side override of server state
  }
}
```

**Recommendation:** This should be a **derived/cached server state** not a writable store. Consider using a proper data-fetching pattern.

#### Issue: rollQueueStore Mixes Server & Client State
**File:** `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`

**Analysis:**
- `rollQueueStore` (lines 3, 158-182) → **SERVER STATE** - sourced from `window.game.rollQueue`
- `stagedResultsStore` (lines 4, 209-220) → **CLIENT STATE** - UI-only staging area
- `stagedMapStore` (lines 5, 204-207) → **CLIENT STATE** - UI-only lookup cache

**Problem:** Mixing server and client state in the same module creates confusion about source of truth.

**Line 40-44 in RollQueue.svelte:**
```javascript
function handleRollQueueUpdate(event) {
  const { rollQueue = [], updatedAt } = event.detail || {};
  setRollQueue(rollQueue);  // ⚠️ Overwrites store with server data
  lastUpdatedStore.set(updatedAt || new Date().toISOString());
}
```

Server pushes → client overwrites → no merge strategy.

### 4. HARDCODED VALUES THAT SHOULD BE DYNAMIC

#### Issue: Default Campaign ID
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Line:** 248

```javascript
if (campaign) {
  campaignId = campaign;
} else if (!campaignId) {
  campaignId = 'test-silverpeak';  // ⚠️ HARDCODED DEFAULT
}
```

**Also appears in:**
- `/opt/dnd/ui-svelte/src/App.svelte` (line 5)
- `/opt/dnd/ui-svelte/src/lib/components/GameArea.svelte` (line 10)
- `/opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte` (line 12)
- `/opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte` (line 16)

**Impact:** Cannot switch campaigns without hardcoding changes.

#### Issue: Poll Delay Hardcoded
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Line:** 164

```javascript
let pollDelay = 2000;  // ⚠️ HARDCODED - should be configurable
```

**Impact:** Cannot adjust polling frequency based on network conditions or user preference.

#### Issue: Hardcoded API Endpoints
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Lines:** 17-24

```javascript
const defaults = [
  '/dnd-api/dnd/combat-state',
  '/dnd/api/dnd/combat-state',
  '/api/dnd/combat-state',
  '/dnd-api/dnd/combat/state',
  '/dnd/api/dnd/combat/state',
  '/api/dnd/combat/state'
].map(ensureUrl).filter(Boolean);
```

**Impact:** Adding new endpoints requires code changes. Should be fully driven by config.

#### Issue: Hardcoded Error Log Throttle
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js**
**Line:** 226

```javascript
if (!endpointUnavailable || now - lastLogTimestamp > 30000) {  // ⚠️ 30 second hardcode
  console.error('combatStore fetch failed:', error);
  lastLogTimestamp = now;
}
```

### 5. STORES DEFINED BUT NEVER USED

#### Issue: defaultCombatState Export
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Line:** 311

```javascript
export { defaultCombatState, normalizeCombatState };
```

**Usage Check:**
- `defaultCombatState` imported in: 
  - `/opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte` (line 5) ✅ **USED** (line 18, 73)

**Status:** ✅ **Actually used**

#### Issue: Unused Derived Stores?
**File:** `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`
**Lines:** 255-313, 315-339

Need to check usage:
- `participantRollStatus` (line 255) → ✅ **USED** in CombatTracker.svelte (line 9, 123)
- `rollSummaryList` (line 315) → ✅ **USED** in CombatTracker.svelte (line 10, 124)

**Status:** ✅ **All exports are used**

### 6. CIRCULAR DEPENDENCIES BETWEEN STORES

#### Analysis:
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
- Imports: `import { writable } from 'svelte/store';`
- No imports from other custom stores ✅

**File:** `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`
- Imports: `import { derived, get, writable } from 'svelte/store';`
- No imports from other custom stores ✅

**Status:** ✅ **NO CIRCULAR DEPENDENCIES** between stores themselves.

However, there's an **indirect circular dependency via window.game**:

```
combatStore → (event) → CombatTracker.svelte → window.game → rollQueueStore
rollQueueStore → (event) → RollQueue.svelte → window.game.submitRollResult → combatStore update
```

This creates a **temporal circular dependency** through global event bus.

### 7. STORE INITIALIZATION ISSUES

#### Issue: combatStore Singleton State
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Lines:** 162-167

```javascript
let campaignId = null;
let pollIntervalId = null;
let pollDelay = 2000;
let inFlight = false;
let endpointUnavailable = false;
let lastLogTimestamp = 0;
```

**Problem:** These are closure variables in the factory function. They persist across all usages of `combatStore`.

**Line 310:**
```javascript
export const combatStore = createCombatStore();  // ⚠️ SINGLETON - shared state
```

**Impact:**
- If multiple components call `combatStore.start()` with different campaigns, they'll interfere
- `pollIntervalId` is shared - stopping in one component stops for all
- `inFlight` flag affects all callers

**Evidence from CombatTracker.svelte (line 122):**
```javascript
$: combatStore.setCampaign(campaign, { fetch: false });
```

This reactive statement runs on every `campaign` change but shares the singleton store instance.

#### Issue: rollQueueStore Lacks Initialization Guard
**File:** `/opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte`
**Lines:** 365-370

```javascript
if (window.game?.rollQueue) {
  setRollQueue(window.game.rollQueue);
  lastUpdatedStore.set(new Date().toISOString());
} else {
  window.game?.refreshRollQueue?.(true);  // ⚠️ Async - may never complete
}
```

**Problem:** No fallback if `window.game` never initializes or `refreshRollQueue()` fails.

### 8. IMPROPER STORE USAGE IN COMPONENTS

#### Issue: Manual Subscribe Instead of Auto-Subscribe
**File:** `/opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte`
**Lines:** 102-106, 123-124

**Problem:**
```javascript
// Manual subscription (lines 102-106)
unsubscribe = combatStore.subscribe(({ state, loading, error: storeError }) => {
  applyCombatState(state);
  storeLoading = loading;
  error = storeError;
});

// But elsewhere uses auto-subscribe syntax (lines 123-124)
$: rollStatusMap = $participantRollStatus;
$: summaryItems = $rollSummaryList;
```

**Why this matters:**
- Manual `subscribe()` requires cleanup and is verbose
- Auto-subscribe `$store` syntax is idiomatic Svelte and handles cleanup automatically
- Mixing patterns creates inconsistent code style

**Better approach:**
```javascript
$: ({ state, loading, error } = $combatStore);
$: applyCombatState(state);
```

#### Issue: Derived Store in Component Instead of Store File
**File:** `/opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte`
**Lines:** 21-27

```javascript
const pendingEntries = derived(rollQueueStore, ($queue) =>
  $queue.filter((entry) => entry.status === 'pending' || entry.status === 'partial')
);

const resolvedEntries = derived(rollQueueStore, ($queue) =>
  $queue.filter((entry) => entry.status === 'complete' || entry.status === 'cancelled')
);
```

**Problem:** These derived stores are defined in the component but should be in `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js` for reusability.

**Current usage:** Only used in RollQueue.svelte
**Recommendation:** If truly component-specific, these are fine. But if other components need pending/resolved lists, move to store file.

#### Issue: Direct `get()` Usage Instead of Reactive
**File:** `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js`
**Lines:** 190, 243-244

```javascript
export function stageResultForInjection(entry, participant) {
  // ...
  const stagedMap = get(stagedMapStore);  // ⚠️ NON-REACTIVE read
  
  if (stagedMap[key]) {
    return { staged: false, reason: 'Already staged' };
  }
  // ...
}

export function getStagedResults() {
  return get(stagedResultsStore);  // ⚠️ NON-REACTIVE read
}
```

**Problem:** Using `get()` breaks reactivity. If these functions are called from components, they won't re-run when stores update.

**Impact:**
- In GameArea.svelte (line 219, 371): `getStagedResults()` won't react to changes
- Must manually track when to re-call

**Better approach:** Export the store directly and let components subscribe.

---

## ADDITIONAL OBSERVATIONS

### Global State Pollution
Both stores rely heavily on `window` globals:
- `window.__CLAUDE_API_CONFIG__` (combatStore.js:5)
- `window.claudeAPI` (combatStore.js:5)
- `window.game` (RollQueue.svelte:365, 369)
- `window.dispatchEvent()` for cross-component communication (rollQueueStore.js:342, GameArea.svelte: many lines)

**Problem:** This creates tight coupling and makes testing difficult.

### No TypeScript
All stores are `.js` instead of `.ts`. No type safety for:
- Store shapes
- Function parameters
- Return values

**Impact:** Runtime errors that could be caught at compile time.

### Missing SSR Guards
**File:** `/opt/dnd/ui-svelte/src/stores/combatStore.js`
**Line:** 5

```javascript
const config = typeof window !== 'undefined' ? (window.__CLAUDE_API_CONFIG__ || ...) : {};
```

✅ **Good** - Has SSR guard

But missing in other places like `rollQueueStore.js` where `window.dispatchEvent` is used without guards (line 342).

---

## SEVERITY RATINGS

### Critical (Fix Immediately)
1. **Race condition in combatStore polling** - Can cause state corruption
2. **Race condition in setRollQueue** - Multiple update sources conflict
3. **combatStore singleton with shared closure state** - Multi-component interference

### High (Fix Soon)
4. **Mixing server and client state** - Architectural debt
5. **Hardcoded campaign IDs** - Prevents multi-campaign support
6. **No SSR guard in rollQueueStore** - Will break on server-side rendering

### Medium (Technical Debt)
7. **Manual subscribe instead of auto-subscribe** - Code inconsistency
8. **Direct `get()` usage breaks reactivity** - Subtle bugs
9. **Missing TypeScript** - No type safety

### Low (Nice to Have)
10. **Hardcoded poll delay** - Configuration improvement
11. **Hardcoded error throttle** - Configuration improvement
12. **Derived stores in components** - Organization improvement

---

## RECOMMENDATIONS

### Immediate Actions:
1. **Remove polling from combatStore** - Rely purely on event-driven updates
2. **Add transaction/lock mechanism to setRollQueue** - Prevent concurrent updates
3. **Split server state from client state** - Create separate stores or use proper cache library
4. **Make campaign a top-level context** - Not prop-drilled through every component

### Architectural Improvements:
1. **Use Svelte Context API** for campaign instead of props
2. **Convert to TypeScript** for type safety
3. **Implement proper state management pattern** (consider Tanstack Query or similar for server state)
4. **Add unit tests** for store logic

### Quick Wins:
1. Move hardcoded values to config object
2. Add SSR guards consistently
3. Use auto-subscribe `$store` syntax everywhere
4. Move reusable derived stores to store files

---

## FILES ANALYZED

### Store Files:
- `/opt/dnd/ui-svelte/src/stores/combatStore.js` (312 lines)
- `/opt/dnd/ui-svelte/src/stores/rollQueueStore.js` (349 lines)

### Component Files:
- `/opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte` (1075 lines)
- `/opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte` (979 lines)
- `/opt/dnd/ui-svelte/src/lib/components/GameArea.svelte` (469 lines)
- `/opt/dnd/ui-svelte/src/App.svelte` (26 lines)

**Total Issues Found:** 10 critical/high, 5 medium/low
**Total Lines Audited:** ~3,200 lines


---

Now let me compile the comprehensive audit report based on all the files I've read:

# Deep Dive Audit Report: Svelte Components in /opt/dnd/ui-svelte/src/

## Summary
I've conducted a thorough audit of all 17 Svelte component files and 2 store files. Below are the specific issues found, categorized by severity.

---

## 1. HARDCODED CAMPAIGN REFERENCES (CRITICAL)

### /opt/dnd/ui-svelte/src/App.svelte
**Lines 5-6:**
```svelte
const campaign = 'test-silverpeak';
```
**Issue:** Campaign is hardcoded instead of being passed dynamically from URL params or application state.
**Impact:** Cannot switch between campaigns without code changes.

### /opt/dnd/ui-svelte/src/lib/components/GameInput.svelte
**Line 5:**
```svelte
export const campaign = "test-silverpeak";
```
**Issue:** Using `export const` instead of `export let` makes this immutable. Also hardcoded value.
**Impact:** Component cannot receive dynamic campaign value from parent.

### /opt/dnd/ui-svelte/src/stores/combatStore.js
**Line 248:**
```javascript
campaignId = 'test-silverpeak';
```
**Issue:** Fallback campaign is hardcoded.
**Impact:** Will always default to test-silverpeak if no campaign specified.

---

## 2. API ENDPOINT MISMATCHES

### /opt/dnd/ui-svelte/src/lib/components/AIProviderSettings.svelte
**Lines 38, 61:**
```svelte
const response = await fetch(`/dnd-api/dnd/ai-provider?campaign=${campaign}`);
// ...
const response = await fetch('/dnd-api/dnd/ai-provider', {
```
**Issue:** Hardcoded endpoint `/dnd-api/dnd/ai-provider` with no fallback mechanism.

### /opt/dnd/ui-svelte/src/lib/components/CampaignManager.svelte
**Lines 24, 54, 63, 85:**
```svelte
const response = await fetch(`/dnd-api/dnd/state?campaign=${campaign}`);
const equipResponse = await fetch(`/dnd-api/dnd/equipment/${encodedChar}?campaign=${campaign}`);
const response = await fetch(`/dnd-api/dnd/equipment/${equipmentId}?campaign=${campaign}`, {
```
**Issue:** Multiple hardcoded `/dnd-api/dnd/` endpoints without fallback.

### /opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte
**Lines 26-44:**
```javascript
function buildEndpointCandidates(path) {
  const config = window.__CLAUDE_API_CONFIG__ || {};
  const bases = Array.from(new Set([
    config.base_url,
    config.fallback_base_url,
    '/dnd-api/dnd',
    '/dnd/api/dnd',
    '/api/dnd'
  ].filter(Boolean)));
```
**Good Practice:** This component has a proper endpoint fallback mechanism that others lack.

### /opt/dnd/ui-svelte/src/stores/combatStore.js
**Lines 17-24:**
```javascript
const defaults = [
  '/dnd-api/dnd/combat-state',
  '/dnd/api/dnd/combat-state',
  '/api/dnd/combat-state',
  '/dnd-api/dnd/combat/state',
  '/dnd/api/dnd/combat/state',
  '/api/dnd/combat/state'
].map(ensureUrl).filter(Boolean);
```
**Good Practice:** Has multiple endpoint fallbacks.

**Recommendation:** All components should use a centralized endpoint configuration like CombatTracker and combatStore.

---

## 3. PROPS NOT BEING USED OR PASSED INCORRECTLY

### /opt/dnd/ui-svelte/src/lib/components/DiceRollPrompt.svelte
**Lines 6-7:**
```svelte
export let rollDetails = {};
export let onRoll = () => {};
```
**Issue:** Component is imported but never used in the application (lines 403-408 in GameArea.svelte show it conditionally rendered but `showDicePrompt` is never set to true).

### /opt/dnd/ui-svelte/src/lib/components/EquipmentManager.svelte
**Lines 4-5:**
```svelte
export let campaign = 'test-silverpeak';
export let character = null;
```
**Lines 65-67:**
```svelte
$: if (character) {
  loadData();
}
```
**Issue:** Reactive statement at line 65 causes `loadData()` to run whenever `character` changes, but `onMount` also calls it (lines 57-62), causing duplicate API calls on component initialization.

### /opt/dnd/ui-svelte/src/lib/components/CampaignManager.svelte
**Lines 99-105:**
```svelte
$: if (selectedCharacter) {
  loadCharacterData();
  // Dispatch custom event when character changes
  window.dispatchEvent(new CustomEvent('characterSelected', {
    detail: { character: selectedCharacter }
  }));
}
```
**Issue:** Reactive statement fires on every change to `selectedCharacter`, including initialization, causing extra API calls.

---

## 4. REACTIVE STATEMENTS THAT MIGHT CAUSE INFINITE LOOPS

### /opt/dnd/ui-svelte/src/lib/components/CampaignNotes.svelte
**Lines 45-51:**
```svelte
onMount(() => {
  saveTimeInterval = setInterval(() => { if (lastSaved) lastSaved = lastSaved; }, 5000);
  return () => {
    clearInterval(saveTimeInterval);
    if (saveTimeout) clearTimeout(saveTimeout);
  };
});
```
**Issue:** Line 46 does `lastSaved = lastSaved` which is a no-op that triggers reactivity without actually changing the value. This is intended to trigger a re-render for the time display but is inefficient.
**Better approach:** Use a separate reactive counter variable.

### /opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte
**Line 122:**
```svelte
$: combatStore.setCampaign(campaign, { fetch: false });
```
**Issue:** Reactive statement calls `setCampaign()` every time `campaign` changes. However, `setCampaign()` has a guard (line 290-293 in combatStore.js) to prevent unnecessary work.
**Status:** Safe, but could be more explicit.

---

## 5. MEMORY LEAKS (SUBSCRIPTIONS NOT CLEANED UP)

### /opt/dnd/ui-svelte/src/lib/components/CombatTracker.svelte
**Lines 101-120:**
```javascript
onMount(() => {
  unsubscribe = combatStore.subscribe(({ state, loading, error: storeError }) => {
    // ...
  });
  
  window.addEventListener('combatStateUpdate', handleCombatStateChange);
  window.addEventListener('turnPrompt', handleTurnPromptEvent);

  return () => {
    window.removeEventListener('combatStateUpdate', handleCombatStateChange);
    window.removeEventListener('turnPrompt', handleTurnPromptEvent);
    combatStore.stop();
    unsubscribe?.();
    document.body.classList.remove('combat-mode');
  };
});
```
**Status:** ✅ GOOD - Properly cleaned up.

### /opt/dnd/ui-svelte/src/lib/components/GameArea.svelte
**Lines 93-119:**
```javascript
onMount(() => {
  window.addEventListener('requestDiceRoll', handleDiceRollRequest);
  window.addEventListener('playerAction', handlePlayerAction);
  window.addEventListener('legacyGameMessage', handleLegacyMessage);
  window.addEventListener('newGameMessage', monitorNewGameMessage);

  combatUnsubscribe = combatStore.subscribe(({ state }) => {
    combatState = state;
  });

  interceptLegacySystem();

  return () => {
    window.removeEventListener('requestDiceRoll', handleDiceRollRequest);
    window.removeEventListener('playerAction', handlePlayerAction);
    window.removeEventListener('legacyGameMessage', handleLegacyMessage);
    window.removeEventListener('newGameMessage', monitorNewGameMessage);
    combatUnsubscribe?.();
  };
});
```
**Status:** ✅ GOOD - Properly cleaned up.

### /opt/dnd/ui-svelte/src/lib/components/GameLog.svelte
**Lines 15-33:**
```javascript
onMount(() => {
  window.addEventListener('newGameMessage', handleNewMessage);
  window.addEventListener('removeThinkingMessage', removeThinkingMessage);
  window.addEventListener('showRollPrompt', handleShowRollPrompt);

  loadExistingMessages();

  return () => {
    window.removeEventListener('newGameMessage', handleNewMessage);
    window.removeEventListener('removeThinkingMessage', removeThinkingMessage);
    window.removeEventListener('showRollPrompt', handleShowRollPrompt);
  };
});
```
**Status:** ✅ GOOD - Properly cleaned up.

### /opt/dnd/ui-svelte/src/lib/components/PartyCredits.svelte
**Lines 11-18:**
```javascript
onMount(async () => {
  await loadCredits();

  // Poll for updates every 5 seconds
  const interval = setInterval(loadCredits, 5000);

  return () => clearInterval(interval);
});
```
**Status:** ✅ GOOD - Properly cleaned up.

### /opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte
**Lines 358-378:**
```javascript
onMount(() => {
  window.addEventListener('rollQueueUpdate', handleRollQueueUpdate);
  focusHandler = (event) => {
    focusEntry(event.detail?.queueId);
  };
  window.addEventListener('rollQueueFocus', focusHandler);

  // ...
});

onDestroy(() => {
  window.removeEventListener('rollQueueUpdate', handleRollQueueUpdate);
  if (focusHandler) {
    window.removeEventListener('rollQueueFocus', focusHandler);
  }
});
```
**Status:** ✅ GOOD - Properly cleaned up.

**Overall:** No memory leaks detected. All subscriptions and event listeners are properly cleaned up.

---

## 6. RACE CONDITIONS IN DATA FETCHING

### /opt/dnd/ui-svelte/src/lib/components/CampaignManager.svelte
**Lines 22-44, 46-81:**
```javascript
async function loadCharacters() {
  try {
    const response = await fetch(`/dnd-api/dnd/state?campaign=${campaign}`);
    // ...
    if (characters.length > 0 && !selectedCharacter) {
      selectedCharacter = characters[0].name;
    }
  }
}

async function loadCharacterData() {
  if (!selectedCharacter) return;
  try {
    loading = true;
    // ... two separate fetch calls
    const equipResponse = await fetch(`/dnd-api/dnd/equipment/${encodedChar}?campaign=${campaign}`);
    const stateResponse = await fetch(`/dnd-api/dnd/state?campaign=${campaign}`);
  }
}
```
**Issue:** Lines 95-96 call both functions sequentially in `onMount`, but then the reactive statement at line 99 also calls `loadCharacterData()` when `selectedCharacter` changes (which happens in `loadCharacters`).
**Race condition:** If user quickly changes `selectedCharacter` before initial load completes, multiple concurrent API calls can occur with no cancellation mechanism.

### /opt/dnd/ui-svelte/src/lib/components/EquipmentManager.svelte
**Lines 13-36, 57-67:**
```javascript
async function loadData() {
  if (!character) return;
  try {
    loading = true;
    // fetch...
  }
}

onMount(() => {
  window.addEventListener('characterSelected', handleCharacterSelected);
});

$: if (character) {
  loadData();
}
```
**Issue:** Same pattern - both event handler (line 53) and reactive statement (line 65) call `loadData()`. No cancellation for in-flight requests.

### /opt/dnd/ui-svelte/src/lib/components/GameLog.svelte
**Lines 215-232:**
```javascript
// Send initiative rolls and wait for response
const response = await window.claudeAPI.sendMessage(fullMessage, 'ic', false);
```
**Issue:** No AbortController to cancel requests if user navigates away or component unmounts during async operation.

**Recommendation:** Implement AbortController pattern for all async fetch operations to prevent race conditions.

---

## 7. DUPLICATE STATE MANAGEMENT

### Multiple Components: Campaign Prop
**Files affected:**
- App.svelte (line 5)
- GameInput.svelte (line 5) 
- AIProviderSettings.svelte (line 6)
- CampaignManager.svelte (line 5)
- CampaignNotes.svelte (line 4)
- CombatTracker.svelte (line 16)
- EquipmentManager.svelte (line 4)
- GameArea.svelte (line 10)
- GameLog.svelte (line 7)
- PartyCredits.svelte (line 4)
- RecentRolls.svelte (line 4)
- RollQueue.svelte (line 12)
- SceneGenerator.svelte (line 6)

**Issue:** Campaign is passed as a prop through the component tree but also hardcoded in multiple places. Should use a centralized store.

### Combat State Duplication
**CombatTracker.svelte** (line 18):
```javascript
let combatState = defaultCombatState;
```
**GameArea.svelte** (line 16):
```javascript
let combatState = null;
```
Both components maintain local copies of combat state from the store subscription, which is correct. However, there's inconsistency - CombatTracker uses `defaultCombatState` while GameArea uses `null`.

### Roll Queue State
**RollQueue.svelte** uses stores properly (rollQueueStore, stagedResultsStore), but also maintains local state for UI-specific concerns (lines 14-19):
```javascript
let submittingMap = {};
let errorMessage = null;
let focusedQueueId = null;
let __outcome = null;
```
**Status:** ✅ GOOD - This is appropriate separation of concerns.

---

## 8. DEAD IMPORTS OR UNUSED CODE

### /opt/dnd/ui-svelte/src/lib/components/CampaignManager.svelte
**Lines 3, 9:**
```svelte
import PartyCredits from './PartyCredits.svelte';
let activeTab = 'equipment'; // equipment, inventory, spells
```
**Issue:** `activeTab` is declared but never used in the template or logic. Component doesn't have tab functionality - that's in EquipmentManager.svelte.

### /opt/dnd/ui-svelte/src/lib/components/GameArea.svelte
**Lines 6-7:**
```svelte
import DiceRollPrompt from './DiceRollPrompt.svelte';
// ...
let showDicePrompt = false;
let currentRollDetails = {};
```
**Lines 403-408:**
```svelte
{#if showDicePrompt}
  <DiceRollPrompt
    rollDetails={currentRollDetails}
    onRoll={handleDiceRoll}
  />
{/if}
```
**Issue:** DiceRollPrompt is imported and conditionally rendered, but `showDicePrompt` is never set to `true`. The handling was disabled as shown in comments (lines 148-161). This is dead code.

### /opt/dnd/ui-svelte/src/lib/components/GameArea.svelte
**Lines 121-178:**
```javascript
function interceptLegacySystem() {
  // Wait for the legacy game object to be available
  const checkInterval = setInterval(() => {
    if (window.game && window.game.addLogEntry) {
      // ...DISABLED sections commented out (lines 148-170)
    }
  }, 100);
}
```
**Issue:** Large sections of interceptLegacySystem are commented out (DISABLED). This technical debt should be cleaned up.

### /opt/dnd/ui-svelte/src/lib/components/CampaignManager.svelte
**Lines 339-597:**
Massive amount of unused CSS (336+ lines) with `/* svelte-ignore css-unused-selector */` comments:
```css
/* svelte-ignore css-unused-selector */
.details-panel {
  display: flex;
  flex-direction: column;
}
/* ... 336 more lines of unused CSS ... */
```
**Issue:** This CSS is left over from removed template code. Should be deleted.

### /opt/dnd/ui-svelte/src/lib/components/RollQueue.svelte
**Line 19:**
```javascript
let __outcome = null;
```
**Lines 546-560:**
```svelte
{#if (__outcome = getAttackOutcome(entry))}
  <span class={`result-tag ${__outcome.crit ? 'crit' : ...}`}>
```
**Issue:** Using assignment in template condition is a code smell. The `__outcome` variable exists only to capture the return value within the template, but this pattern is error-prone.

### /opt/dnd/ui-svelte/src/lib/components/TypewriterText.svelte
**Lines 2-4:**
```javascript
import { onMount, createEventDispatcher } from 'svelte';

const dispatch = createEventDispatcher();
```
**Status:** ✅ GOOD - Used properly.

---

## 9. ADDITIONAL ISSUES FOUND

### GameInput.svelte - Const instead of Let
**Line 5:**
```svelte
export const campaign = "test-silverpeak";
```
**Issue:** Should be `export let` to allow parent to pass value.

### GameLog.svelte - Complex Logic in Template
**Lines 368-389:**
```svelte
{#each messages as message, i (message.id)}
  <div class="log-entry {message.role}-entry"
    class:dm-entry={message.role === 'assistant'}
    class:player-entry={message.role === 'user'}
    data-mode={message.mode}
    in:fly={{ y: 20, duration: 400, easing: quintOut }}
  >
    <!-- ... -->
    {#if message.isTyping && i === currentTypingIndex}
      <TypewriterText text={formatMessage(message.content)} on:complete={handleTypewriterComplete} />
    {:else}
      {@html formatMessage(message.content)}
    {/if}
  </div>
{/each}
```
**Issue:** Calling `formatMessage()` twice per message (once for TypewriterText, potentially another for @html). Should memoize or compute once.

### SceneGenerator.svelte - Missing Error Boundary
**Lines 38-65:**
```javascript
async function generateScene() {
  loading = true;
  error = null;
  sceneDescription = '';

  try {
    // ...
    const response = await fetch('/dnd-api/dnd/generate-scene', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ campaign })
    });
```
**Issue:** No timeout on fetch, could hang indefinitely. No AbortController.

### HelpModal.svelte - Hardcoded Party Members
**Line 52:**
```svelte
<li>👥 Three party members: Kira, Thorne, and Riven</li>
```
**Issue:** Party members are hardcoded in help text. Should be dynamically loaded or at least configurable.

---

## SEVERITY SUMMARY

### CRITICAL (Must Fix)
1. Hardcoded campaign references in 13+ files
2. Race conditions in data fetching (CampaignManager, EquipmentManager)
3. API endpoint mismatches without fallback mechanisms

### HIGH (Should Fix)
1. GameInput.svelte using `export const` instead of `export let`
2. Dead code: DiceRollPrompt component never shown
3. 336+ lines of unused CSS in CampaignManager
4. Duplicate API calls from reactive statements + onMount

### MEDIUM (Consider Fixing)
1. No AbortController for canceling in-flight requests
2. Inefficient reactivity in CampaignNotes (no-op assignment)
3. Campaign prop duplication (should use centralized store)
4. Complex formatMessage() logic called multiple times

### LOW (Nice to Have)
1. Assignment in template condition (__outcome)
2. Commented-out code in interceptLegacySystem
3. Hardcoded party members in HelpModal

---

## RECOMMENDATIONS

1. **Create a Campaign Store**: Replace hardcoded campaign values with a centralized Svelte store
2. **Centralized API Configuration**: Use endpoint fallback pattern from CombatTracker everywhere
3. **Add AbortController**: Implement request cancellation for all async operations
4. **Remove Dead Code**: Delete DiceRollPrompt import and unused CSS in CampaignManager
5. **Fix GameInput**: Change `export const` to `export let campaign`
6. **Debounce Reactive Statements**: Add debouncing to prevent duplicate API calls
7. **Memoize Expensive Functions**: Cache results of `formatMessage()` and similar functions


---

Excellent! Now I have all the information I need. Let me create a comprehensive audit report:

---

## Deep Dive Audit Report: D&D Campaign Manager HTML Files

### Files Examined:
- **/opt/dnd/game.html** (main/root)
- **/opt/dnd/campaigns/dax/index.html**
- **/opt/dnd/campaigns/test-silverpeak/index.html**

---

## CRITICAL ISSUES

### 1. **MISSING SCRIPT FILES - BROKEN DEPENDENCIES**
**Severity: CRITICAL** 

**Problem:** Both `game.html` and `dax/index.html` reference JavaScript files that **DO NOT EXIST**:

```html
<!-- game.html and dax/index.html (lines 471-472 / 505-506) -->
<script src="api-handler.js?v=1764054345&cf=dev"></script>
<script src="script.js?v=1764054345&cf=dev"></script>
```

**Reality Check:**
- `/opt/dnd/api-handler.js` - **DOES NOT EXIST**
- `/opt/dnd/script.js` - EXISTS but contains `DNDCampaign` class (legacy)
- Correct files are in `/opt/dnd/shared/`:
  - `/opt/dnd/shared/api-handler-base.js`
  - `/opt/dnd/shared/campaign-base.js`

**Impact:** 
- `game.html` will fail to load `api-handler.js` (404 error)
- `window.claudeAPI` won't be initialized
- All API calls will fail
- The inline DOMContentLoaded script attempts to override `window.claudeAPI.*` methods that don't exist yet

---

### 2. **INCONSISTENT PATH CONVENTIONS**
**Severity: HIGH**

**game.html** uses relative paths:
```html
<link rel="stylesheet" href="styles.css?v=1764054345">
<script src="api-handler.js?v=1764054345&cf=dev"></script>
<script src="script.js?v=1764054345&cf=dev"></script>
```

**dax/index.html** mixes relative and absolute:
```html
<link rel="stylesheet" href="/dnd/styles.css?v=1764054345">  <!-- absolute -->
<script src="api-handler.js?v=1764054345&cf=dev"></script>    <!-- relative -->
<script src="script.js?v=1764054345&cf=dev"></script>         <!-- relative -->
```

**test-silverpeak/index.html** uses consistent absolute paths:
```html
<link rel="stylesheet" href="/dnd/styles.css?v=1764054775">
<script src="/dnd/shared/api-handler-base.js?v=1764054775"></script>
<script src="/dnd/shared/campaign-base.js?v=1764054775"></script>
```

**Impact:**
- Relative paths in `dax/index.html` will look for `/opt/dnd/campaigns/dax/api-handler.js` (wrong location)
- Inconsistent behavior when serving from different directories
- Cache busting won't work correctly across campaigns

---

### 3. **RACE CONDITION: Script Override Timing**
**Severity: HIGH**

All three files contain this pattern:
```javascript
document.addEventListener('DOMContentLoaded', function() {
    if (window.claudeAPI) {
        // Override sendMessage, parseRollRequest, handleRollRequest
    }
});
```

**Problem Flow:**
1. Scripts load: `<script src="api-handler.js">` or `<script src="/dnd/shared/api-handler-base.js">`
2. At script parse time: `window.claudeAPI = new ClaudeAPIHandler()` (synchronous)
3. Inline script executes (synchronous)
4. `DOMContentLoaded` fires later (async)

**Race Condition:**
- If `api-handler-base.js` loads AFTER the inline script executes, `window.claudeAPI` won't exist when the inline script runs
- The `DOMContentLoaded` check might succeed, but the overrides happen AFTER `DNDCampaign.init()` already tried to call `window.claudeAPI.initialize()`
- The `setInterval` polling for `window.game.handleRollRequest` is a hack to work around initialization order

**Evidence:**
```javascript
// From inline scripts - polling to find window.game
const originalGameHandleRollRequest = setInterval(() => {
    if (window.game && window.game.handleRollRequest) {
        clearInterval(originalGameHandleRollRequest);
        // ...override...
    }
}, 100); // Polls every 100ms until it exists
```

---

### 4. **DUPLICATE AND CONFLICTING INLINE SCRIPTS**
**Severity: MEDIUM**

**Duplicate Logic Across Files:**
All three HTML files contain 100+ lines of nearly identical inline JavaScript:
- Campaign ID extraction
- `window.claudeAPI` override logic
- `window.game.handleRollRequest` override polling
- Svelte conditional loading (even in `dax/index.html` which doesn't use Svelte)

**Conflicts:**
- **game.html** and **dax/index.html**: Use `modeOrIsRollResult` parameter pattern (3-parameter signature)
- **test-silverpeak/index.html**: Uses `isRollResult` parameter pattern (2-parameter signature)

```javascript
// game.html & dax/index.html
window.claudeAPI.sendMessage = async function(playerMessage, modeOrIsRollResult, maybeIsRollResult) {
    let mode = 'ic';
    let isRollResult = false;
    if (typeof modeOrIsRollResult === 'string') {
        mode = modeOrIsRollResult;
        isRollResult = !!maybeIsRollResult;
    } else if (typeof modeOrIsRollResult === 'boolean') {
        isRollResult = modeOrIsRollResult;
    }
    // ...
}

// test-silverpeak/index.html
window.claudeAPI.sendMessage = async function(playerMessage, isRollResult = false) {
    console.log('=== DEBUGGING sendMessage ===');
    console.log('Input:', playerMessage, isRollResult);
    // ...
}
```

**Impact:**
- API inconsistency between campaigns
- When switching campaigns, cached assumptions about API signatures will fail
- Developer confusion about which signature is "correct"

---

### 5. **SVELTE LOADING INCONSISTENCY**
**Severity: MEDIUM**

**game.html** and **dax/index.html** both include:
```javascript
// Conditionally load Svelte Equipment Manager for test-silverpeak
if (CAMPAIGN_ID === 'test-silverpeak') {
    // Load Svelte CSS & JS dynamically
    // ...
}
```

**Problems:**
1. **Wrong context**: These files are for `game.html` (default campaign) and `dax` campaign, not test-silverpeak
2. **URL parameter reliance**: Logic assumes `?campaign=test-silverpeak` param, but users access campaigns via `/dnd/campaigns/test-silverpeak/index.html` directly
3. **Dynamic script injection**: Adds `Date.now()` to Svelte JS path, defeating cache and causing multiple loads

```javascript
// This is problematic
svelteJS.src = '/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-DpGllzxI.js?v=1764054345' + Date.now();
```

**Correct Approach (test-silverpeak):**
```html
<!-- Statically loaded in <head> -->
<link rel="stylesheet" href="/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-6GABUy0N.css?v=1764054775">
<!-- Statically loaded at end of <body> -->
<script type="module" src="/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-DpGllzxI.js"></script>
```

---

### 6. **DOM ELEMENT ARCHITECTURE MISMATCH**
**Severity: MEDIUM**

**game.html & dax/index.html** (Legacy Architecture):
- Full DOM structure with all panels rendered
- Single mount point: `<div id="equipment-app"></div>` (lines 204/238)
- All other UI is vanilla HTML/CSS

**test-silverpeak/index.html** (Svelte Architecture):
- Multiple Svelte mount points:
  - `<div id="header-controls-app"></div>` (line 47)
  - `<aside id="svelte-character-panel">` (line 85)
  - `<main id="game-area-app">` (line 90)
  - `<div id="scene-generator-app"></div>` (line 101)
  - `<div id="equipment-app"></div>` (line 105)
  - `<div id="recent-rolls-app"></div>` (line 110)
  - `<div id="campaign-notes-app"></div>` (line 113)
  - `<div id="combat-tracker-app"></div>` (line 120)
- **Hidden legacy DOM elements** (lines 52-81):
```html
<div style="display: none;">
    <span id="character-name"></span>
    <span id="character-race-class"></span>
    <!-- ...dozens more for legacy JS compatibility... -->
</div>
```

**Impact:**
- test-silverpeak expects Svelte components to mount; legacy JS expects direct DOM access
- Hidden elements are a "compatibility shim" to prevent JS errors when legacy code tries `getElementById()`
- This hybrid approach is fragile and hard to maintain

---

### 7. **HARDCODED IMAGE PATH**
**Severity: LOW**

Both `game.html` and `dax/index.html`:
```html
<img src="dax pfp.png" alt="Dax Stargazer" class="character-image" id="character-image">
```

**Problems:**
- Hardcoded to "dax pfp.png" with space in filename (poor practice)
- Relative path will resolve differently depending on HTML file location
- Should be dynamic based on selected character

**test-silverpeak** handles this better:
```html
<img id="character-image" src="" alt="">  <!-- Empty, filled by JS/Svelte -->
```

---

### 8. **VERSION NUMBER DRIFT**
**Severity: LOW**

- **game.html** & **dax/index.html**: `v=1764054345`
- **test-silverpeak/index.html**: `v=1764054775`

**Gap:** 430 seconds (7.2 minutes) between versions

**Impact:**
- Cache inconsistency between campaigns
- Users may load old cached assets when switching campaigns
- Indicates files were updated separately, not atomically

---

### 9. **LEGACY CODE NOT CLEANED UP**
**Severity: LOW**

**game.html** (lines 290-427):
- Complete character creation modal HTML (300+ lines)
- AI provider selection modal
- About modal

**test-silverpeak** has the same modals but:
- About modal lists wrong characters: "Dax, Chen, and Dr. Yuen" should be "Kira, Thorne, and Riven" (line 280)

---

### 10. **INLINE SCRIPT COMPLEXITY**
**Severity: MEDIUM**

The inline `<script>` blocks (135+ lines each) contain:
- Campaign ID detection
- API method overrides
- Roll request parsing logic
- Conditional Svelte loading
- Debug logging

**This should be:**
- Extracted to a shared `campaign-init.js` file
- Parameterized per campaign (not duplicated)
- Versioned and cache-busted like other scripts

---

## SCRIPT LOADING ORDER ANALYSIS

### game.html & dax/index.html (BROKEN):
```
1. <script src="api-handler.js"> ❌ DOES NOT EXIST
2. <script src="script.js">      ✅ Loads DNDCampaign class
3. <script> inline               ✅ Tries to override non-existent window.claudeAPI
4. DOMContentLoaded              ❌ window.claudeAPI still undefined
```

### test-silverpeak/index.html (WORKING):
```
1. <script src="/dnd/shared/api-handler-base.js">     ✅ Creates window.claudeAPI
2. <script src="/dnd/shared/campaign-base.js">        ✅ Defines CampaignBase class
3. <script src=".../campaign-config.js">              ✅ Instantiates campaign
4. <script> inline                                     ✅ Overrides work (claudeAPI exists)
5. <script type="module" src=".../svelte/...">        ✅ Mounts Svelte components
```

---

## RECOMMENDATIONS

### IMMEDIATE (Fix Broken Campaigns):

1. **Fix game.html and dax/index.html script references:**
   ```html
   <!-- Change from: -->
   <script src="api-handler.js?v=1764054345&cf=dev"></script>
   <script src="script.js?v=1764054345&cf=dev"></script>
   
   <!-- To: -->
   <script src="/dnd/shared/api-handler-base.js?v=CURRENT_VERSION"></script>
   <script src="/dnd/shared/campaign-base.js?v=CURRENT_VERSION"></script>
   <script src="/dnd/campaigns/dax/campaign-config.js?v=CURRENT_VERSION"></script>
   ```

2. **Standardize all paths to absolute:**
   - All CSS: `/dnd/styles.css`
   - All JS: `/dnd/shared/...` or `/dnd/campaigns/.../...`
   - All images: `/dnd/campaigns/.../images/...`

3. **Extract inline scripts to shared file:**
   - Create `/dnd/shared/campaign-overrides.js`
   - Parameterize by campaign ID
   - Load AFTER campaign initialization

### SHORT-TERM (Architecture Cleanup):

4. **Remove duplicate inline scripts:**
   - Single source of truth for override logic
   - Campaign-specific config, not campaign-specific code

5. **Fix API signature inconsistency:**
   - Decide on one signature for `sendMessage()`
   - Update all campaigns to match

6. **Remove Svelte conditional loading from non-Svelte campaigns:**
   - `game.html` and `dax/index.html` should NOT have test-silverpeak Svelte logic

7. **Synchronize version numbers:**
   - Use build script to update all HTML files atomically
   - Consider single `version.js` imported by all pages

### LONG-TERM (Refactoring):

8. **Migrate all campaigns to Svelte architecture:**
   - test-silverpeak is the template
   - Eliminates dual architecture maintenance

9. **Create HTML template system:**
   - Single base template
   - Campaign-specific variables injected at build/serve time
   - No duplicate 600-line HTML files

10. **Implement proper module system:**
    - ES6 modules with explicit imports
    - No reliance on global `window.*` assignments
    - Better TypeScript support

---

## SUMMARY TABLE

| Issue | game.html | dax/index.html | test-silverpeak/index.html |
|-------|-----------|----------------|----------------------------|
| Missing script files | ❌ Critical | ❌ Critical | ✅ Correct |
| Path consistency | ❌ Relative | ⚠️ Mixed | ✅ Absolute |
| Script load order | ❌ Broken | ❌ Broken | ✅ Working |
| Inline script duplication | ⚠️ 135 lines | ⚠️ 135 lines | ⚠️ 127 lines |
| API signature consistency | ⚠️ 3-param | ⚠️ 3-param | ⚠️ 2-param |
| Svelte mount points | ✅ 1 | ✅ 1 | ✅ 8 |
| Hidden compatibility DOM | ✅ N/A | ✅ N/A | ⚠️ Present |
| Campaign-specific config | ❌ Inline | ❌ Inline | ✅ Separate file |
| Version number | 1764054345 | 1764054345 | 1764054775 |

**Legend:**
- ✅ Correct/Working
- ⚠️ Suboptimal but functional
- ❌ Broken or critical issue

---

## CONCLUSION

The **test-silverpeak/index.html** represents the **correct architecture**, while **game.html** and **dax/index.html** are fundamentally broken due to missing script dependencies. The test-silverpeak campaign is likely working because it uses the correct shared base scripts and follows the VodBase pattern properly. The other two campaigns will fail to initialize `window.claudeAPI` and crash on first API call.

**Priority:** Fix the script references in game.html and dax/index.html immediately, then systematically address the architectural inconsistencies to bring all campaigns into alignment with the working test-silverpeak pattern.


---

Perfect! Now I have a complete picture. Let me create a comprehensive audit report:

---

# DEEP DIVE AUDIT: Legacy DAX Campaign Remnants in /opt/dnd

## Executive Summary

The /opt/dnd codebase has been **largely cleaned** of old separate-server DAX campaign infrastructure. The migration from the old `/opt/dnd-dax` standalone server to the unified campaign system appears to be **mostly complete**, but there are still **significant legacy references** that need attention.

## Critical Findings

### 1. **HARDCODED "test-silverpeak" REFERENCES** (42 instances in main server)
The biggest issue is NOT old DAX paths, but rather hardcoded Silverpeak campaign assumptions throughout `/opt/dnd/complete-intelligent-server.js`:

**Location**: `/opt/dnd/complete-intelligent-server.js`

#### A. Database/RAG Initialization (Lines 993-1008)
```javascript
// Line 993-994
if (this.campaignId === 'test-silverpeak') {
    console.log('📊 Initializing database for Silverpeak...');
```
**Impact**: Database and RAG services ONLY work for Silverpeak campaign

#### B. Equipment/HP/Credits API Gates (8 instances, Lines 6532-6849)
```javascript
// Line 6532
if (campaign !== 'test-silverpeak') {
    return res.status(400).json({ error: 'Equipment API only available for Silverpeak campaign' });
}
```
**Found at lines**: 6532, 6586, 6639, 6685, 6727, 6772, 6824, 6849

**Impact**: All equipment, HP, credits, and condition APIs reject non-Silverpeak campaigns

#### C. Hardcoded NPCs/Locations (Lines 1357, 1377, 1776-1783)
```javascript
// Line 1357
'silverpeak ranger',
// Line 1377
'silverpeak',
// Line 1776-1783
return 'Silverpeak Mountains';
return 'Ancient ruins near Silverpeak';
return this.campaignState?.world?.currentLocation || 'Silverpeak Hinterlands';
```

#### D. Default Campaign Setting (Line 3915)
```javascript
const DEFAULT_CAMPAIGN = process.env.DEFAULT_CAMPAIGN || 'test-silverpeak';
```

#### E. Fantasy Campaign Descriptions (Lines 776-777, 855-856)
Hardcoded campaign lore in DM prompts reference Silverpeak specifically

### 2. **OLD PATH REFERENCES: /opt/vodbase/dnd-5e** (57 instances)

**Files affected**:
- `/opt/dnd/ecosystem.config.js` - Line 5: `cwd: '/opt/vodbase/dnd-campaign'` (should be `/opt/dnd`)
- `/opt/dnd/MIGRATION-PLAN-DAX.md` - Multiple references to old paths
- `/opt/dnd/SETUP-MEMORY-SERVICE.md` - Multiple `/opt/vodbase/dnd-5e/` paths
- `/opt/dnd/SILVERPEAK-DATABASE-IMPLEMENTATION.md` - Multiple old path references
- `/opt/dnd/CLAUDE-WORK-PLAN.md` - Historical references
- `/opt/dnd/database/integration-patch.md` - Old path documentation
- `/opt/dnd/rag-service/ecosystem.config.js` - Line 6: old path

**Impact**: PM2 ecosystem config references wrong working directory

### 3. **"dnd-5e" STRING REFERENCES** (2 instances)

**Location**: `/opt/dnd/rollback-simple.html`
- Line 66: `server: 'dnd-5e'`
- Line 123: `return 'dnd-5e';`

**Impact**: Rollback utility still has old server name hardcoded

### 4. **PORT 3001 REFERENCES** (5 instances)

**Files**:
- `/opt/dnd/complete-intelligent-server.js` - Line 6889: `const PORT = process.env.PORT || 3001;` ✅ (This is OK - current port)
- `/opt/dnd/_archive/` - Multiple archived files (safe to ignore)

**Status**: ✅ Current port is 3001, so these are correct

### 5. **DAX CAMPAIGN BACKUP DIRECTORY** 

**Location**: `/opt/dnd/dax-campaign-backup/`

**Contents**:
- 20 backup files including conversation history, campaign state, portraits
- Files like "dax campaign character sheet.txt" and "dax campaign full log.txt"
- Database JSON backups

**Status**: ⚠️ Backup directory exists but is NOT referenced by active code
**Risk**: Medium - could be deleted if backup is no longer needed

### 6. **API PATH INCONSISTENCIES**

Multiple API path patterns found:
- `/dnd-api/dnd/*` - Used in frontend files
- `/api/dnd/*` - Used in server routes  
- `/dnd/api/dnd/*` - Mixed usage in script.js

**Files affected**:
- `/opt/dnd/shared/api-handler-base.js` - Lines 220, 477
- `/opt/dnd/script.js` - Lines 1139, 1169, 1586, 2016
- `/opt/dnd/index.html` - Lines 321, 472
- Frontend svelte components

**Impact**: No immediate issue, but inconsistent patterns

### 7. **LOCALHOST:5003 REFERENCES** (RAG Service)

**Found in**: Documentation files only
- `/opt/dnd/SETUP-MEMORY-SERVICE.md`
- `/opt/dnd/rag-service/README.md`
- `/opt/dnd/complete-intelligent-server.js` - Line 1008 (active code)

**Status**: ✅ This is correct - RAG service runs on port 5003

## What Was Successfully Cleaned

✅ **NO references to**: 
- `/opt/dnd-dax` (old separate server path)
- `/opt/dnd-staging`
- Port 3002 (old staging port)
- `dax-campaign-backup` in active code
- Hardcoded "dax" campaign assumptions

✅ **DAX campaign properly migrated**:
- `/opt/dnd/campaigns/dax/` exists with full campaign data
- Campaign config, state, conversation history all present
- Accessible via `?campaign=dax` parameter

✅ **Campaign system is multi-tenant**:
- Server uses `campaignId` parameter throughout
- Default fallback is `'default'` (not hardcoded to specific campaign)
- Both Silverpeak and Dax campaigns coexist

## Recommendations

### Priority 1: Critical (Blocks Multi-Campaign Functionality)

1. **Remove Silverpeak-specific API gates** (Lines 6532-6849)
   - Equipment APIs should work for ALL campaigns with appropriate data structures
   - Change error messages or use feature detection instead of campaign name checks

2. **Generalize NPC/Location patterns** (Lines 1355-1392)
   - Move Silverpeak-specific patterns to campaign config files
   - Make pattern matching campaign-aware

### Priority 2: Important (PM2 & Infrastructure)

3. **Fix ecosystem.config.js** - Line 5
   - Change `cwd: '/opt/vodbase/dnd-campaign'` to `cwd: '/opt/dnd'`

4. **Update rag-service PM2 config** 
   - `/opt/dnd/rag-service/ecosystem.config.js` line 6

### Priority 3: Cleanup (Documentation & Legacy)

5. **Update documentation files**:
   - Replace all `/opt/vodbase/dnd-5e/` with `/opt/dnd/`
   - Update MIGRATION-PLAN-DAX.md to reflect completed state

6. **Fix rollback-simple.html**:
   - Remove hardcoded 'dnd-5e' server references

7. **Decide on dax-campaign-backup/**:
   - Keep if needed for disaster recovery
   - Delete if migration is verified stable

### Priority 4: Nice to Have

8. **Standardize API paths**:
   - Choose one pattern (`/api/dnd/*` recommended)
   - Update frontend to use consistent paths

## Files Requiring Changes

### Immediate Action Required:
1. `/opt/dnd/complete-intelligent-server.js` - 42 Silverpeak hardcodes
2. `/opt/dnd/ecosystem.config.js` - Wrong working directory
3. `/opt/dnd/rag-service/ecosystem.config.js` - Wrong working directory

### Documentation Updates:
4. `/opt/dnd/MIGRATION-PLAN-DAX.md`
5. `/opt/dnd/SETUP-MEMORY-SERVICE.md`
6. `/opt/dnd/SILVERPEAK-DATABASE-IMPLEMENTATION.md`
7. `/opt/dnd/CLAUDE-WORK-PLAN.md`
8. `/opt/dnd/database/integration-patch.md`

### Optional:
9. `/opt/dnd/rollback-simple.html` - Legacy server name
10. `/opt/dnd/dax-campaign-backup/` - Consider archiving or deleting

## Conclusion

The DAX campaign migration from separate server to unified system is **functionally complete**. However, the codebase still contains **significant Silverpeak-centric assumptions** that limit true multi-campaign functionality. The most critical issue is not old DAX references (which are mostly gone), but rather the **42 hardcoded checks for 'test-silverpeak'** that gate features or make assumptions about campaign structure.

**Migration Score**: 75% complete
- ✅ Old `/opt/dnd-dax` path references: **CLEAN**
- ✅ Old port references (3002): **CLEAN**  
- ⚠️ Campaign-agnostic code: **NEEDS WORK** (Silverpeak hardcodes)
- ⚠️ Infrastructure configs: **NEEDS UPDATE** (wrong paths)
- ⚠️ Documentation: **OUTDATED** (57 old path references)


---

Perfect! Now I have enough information to compile a comprehensive audit report. Let me create the final analysis:

# Deep Dive Audit: Campaign-Specific Files

## Executive Summary
Comprehensive audit of `/opt/dnd/campaigns/dax/` and `/opt/dnd/campaigns/test-silverpeak/` reveals significant architectural differences, missing file references, incomplete migration to shared architecture, and opportunities for code consolidation.

---

## 1. INDEX.HTML FILES - SCRIPT/STYLE LOADING ISSUES

### **DAX Campaign** (`/opt/dnd/campaigns/dax/index.html`)
**CRITICAL ISSUES:**

1. **Missing Script Files** (Lines 505-506):
   - References `api-handler.js` - **FILE DOES NOT EXIST** in campaign directory
   - References `script.js` - **FILE DOES NOT EXIST** in campaign directory
   - These files exist at `/opt/dnd/api-handler.js` and `/opt/dnd/script.js` (root level)
   - **Paths should be**: `/dnd/script.js` and possibly an api-handler file

2. **Incorrect Svelte Hash** (Line 630):
   - Hardcoded: `index-DpGllzxI.js` 
   - Actual file: `index-C8l6J5-K.js`
   - **MISMATCH - Will fail to load Svelte components**

3. **Inline Critical CSS** (Lines 12-43):
   - Has 30+ lines of inline theme variables
   - Duplicates content from `theme.css`
   - Should be consolidated

4. **File Size**: 643 lines - extremely bloated for a campaign-specific file

### **TEST-SILVERPEAK Campaign** (`/opt/dnd/campaigns/test-silverpeak/index.html`)
**BETTER ARCHITECTURE:**

1. **Uses Shared Core** (Lines 307-311):
   ```html
   <script src="/dnd/shared/api-handler-base.js?v=1764054775"></script>
   <script src="/dnd/shared/campaign-base.js?v=1764054775"></script>
   <script src="/dnd/campaigns/test-silverpeak/campaign-config.js?v=1764054775"></script>
   ```
   ✅ All referenced files exist

2. **Correct Svelte References** (Lines 19, 443):
   - CSS: `index-6GABUy0N.css` ✅ EXISTS
   - JS: `index-DpGllzxI.js` ❌ **WRONG HASH** (should be `index-C8l6J5-K.js`)
   - Svelte build output shows correct hash: `index-C8l6J5-K.js`

3. **File Size**: 445 lines - cleaner, uses Svelte components

4. **Hidden Legacy Elements** (Lines 52-81):
   - Has hidden placeholder elements for legacy JS compatibility
   - Smart backward-compatibility approach

---

## 2. HARDCODED PATHS & CAMPAIGN-AWARENESS

### Issues Found:

1. **Dax Campaign - Not Campaign-Aware**:
   - Line 510: `const CAMPAIGN_ID = urlParams.get('campaign') || 'default';`
   - Default should be `'dax'`, not `'default'`

2. **Test-Silverpeak - Better**:
   - Line 316: `const CAMPAIGN_ID = urlParams.get('campaign') || 'test-silverpeak';`
   - ✅ Correct fallback

3. **Relative vs Absolute Paths**:
   - Dax uses: `href="dax pfp.png"` (Line 102) - relative path ✅
   - All `/dnd/` paths are absolute ✅
   - No issues found with path structure

---

## 3. MISSING FILES REFERENCED BUT DON'T EXIST

### Critical Missing Files:

| Referenced File | Referenced By | Status |
|----------------|---------------|--------|
| `/opt/dnd/campaigns/dax/api-handler.js` | dax/index.html:505 | ❌ MISSING |
| `/opt/dnd/campaigns/dax/script.js` | dax/index.html:506 | ❌ MISSING |
| `/opt/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-DpGllzxI.js` | test-silverpeak/index.html:443 & dax/index.html:630 | ❌ WRONG HASH |

### Files That DO Exist:
- ✅ `/opt/dnd/script.js` (root level, 88KB)
- ✅ `/opt/dnd/shared/api-handler-base.js` (22KB)
- ✅ `/opt/dnd/shared/campaign-base.js` (162KB)
- ✅ `/opt/dnd/shared/campaign-base.css` (38KB)
- ✅ `/opt/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-C8l6J5-K.js` (128KB)
- ✅ `/opt/dnd/campaigns/test-silverpeak/svelte-dist/assets/index-6GABUy0N.css` (44KB)

---

## 4. DUPLICATE CODE BETWEEN CAMPAIGNS

### Massive Duplication Identified:

1. **Modal HTML** (3 modals × ~100 lines each):
   - Character Creation Modal (lines 125-213 in both files)
   - AI Provider Settings Modal (lines 216-262 in both files)
   - About Modal (lines 264-304 in both files)
   - **IDENTICAL CODE** in both campaigns
   - **Should be**: Shared template or Svelte components

2. **HTML Structure Differences**:
   - 450 lines differ between the two files
   - Dax has full legacy UI (643 lines)
   - Silverpeak uses Svelte mount points (445 lines)
   - Dax includes 240+ lines of character panel HTML that Silverpeak replaces with `<div id="svelte-character-panel">`

3. **Inline Script Overrides** (Lines 507-611 in Dax, 323-439 in Silverpeak):
   - Nearly identical debugging code
   - `parseRollRequest` override logic duplicated
   - `handleRollRequest` override logic duplicated
   - ~100 lines of duplicate JavaScript

### Code Consolidation Opportunities:

| Component | Dax Lines | Silverpeak Lines | Should Be |
|-----------|-----------|------------------|-----------|
| Modals | ~300 | ~300 | Shared Svelte components |
| Character Panel | ~170 | ~5 (mount point) | Svelte component (Silverpeak approach) |
| Game Area | ~40 | ~5 (mount point) | Svelte component (Silverpeak approach) |
| Tools Panel | ~100 | ~25 (mount points) | Svelte components (Silverpeak approach) |
| Debug Overrides | ~100 | ~100 | Shared base script |

**Estimated Duplicate Code**: ~600+ lines across both campaigns

---

## 5. CSS/THEME FILES - STRUCTURE & COMPLETENESS

### **Dax Theme** (`/opt/dnd/campaigns/dax/theme.css`)
- **Size**: 187 lines
- **Structure**: ✅ Well-organized
- **Completeness**: ✅ Complete sci-fi theme
- **CSS Variables**: 13 custom properties defined
- **Issues**: 
  - Inline CSS in HTML duplicates 30+ lines (lines 12-43 in index.html)

### **Test-Silverpeak Theme** (`/opt/dnd/campaigns/test-silverpeak/theme.css`)
- **Size**: 427 lines
- **Structure**: ✅ Excellent organization with comments
- **Completeness**: ✅ Very complete Dark Grimoire theme
- **CSS Variables**: 18+ custom properties
- **Features**:
  - Combat mode styling (lines 348-427)
  - Animations (`aiPulse`, `mysticalPulse`, `combatBadgePulse`)
  - Responsive grid layouts
- **Issues**:
  - References fonts NOT loaded in HTML:
    - `Cinzel` - ❌ NOT in HTML `<link>` (15 references)
    - `IM Fell English` - ❌ NOT in HTML `<link>` (3 references)
    - Only `Crimson Text` is loaded (line 17 of HTML)

**CRITICAL**: Test-Silverpeak theme.css uses fonts that aren't loaded!

---

## 6. CAMPAIGN-CONFIG.JS FILES

### **Dax Config** (`/opt/dnd/campaigns/dax/campaign-config.js`)
**Status**: ✅ Complete and correct
- 72 lines
- Well-structured configuration object
- All 3 characters defined with stats
- Exports for both browser and Node.js (lines 66-71)
- ✅ No issues

### **Test-Silverpeak Config** (`/opt/dnd/campaigns/test-silverpeak/campaign-config.js`)
**Status**: ⚠️ Functional but has notes
- 95 lines
- More detailed configuration
- Comment on line 2: "Test for Shared Core Architecture"
- Comment on line 19: "Will use placeholder" for images
- Initializes with `CampaignBase` class (lines 87-93)
- ⚠️ All character `image` properties are `null` (no portraits)
- ✅ Otherwise complete

---

## 7. LEGACY REFERENCES TO OLD FOLDER STRUCTURES

### Found Issues:

1. **Dax Campaign**: 
   - Lines 614-641: Conditional loading code checks `if (CAMPAIGN_ID === 'test-silverpeak')`
   - ❌ **Dax's index.html shouldn't know about test-silverpeak**
   - This is legacy cross-campaign awareness

2. **No old path references found** like:
   - ❌ No `/campaigns-old/` references
   - ❌ No `../campaigns/` references
   - ✅ All paths use `/dnd/campaigns/` structure

3. **Placeholder content in Dax**:
   - Lines 183-186: Generic "Whispering Woods" / "Crystal of Echoing Dreams" text
   - Should be replaced with campaign-specific intro

---

## PRIORITY RECOMMENDATIONS

### 🔴 CRITICAL (Fix Immediately):

1. **Fix Dax Script Paths** (dax/index.html:505-506):
   ```html
   <!-- WRONG -->
   <script src="api-handler.js?v=1764054345&cf=dev"></script>
   <script src="script.js?v=1764054345&cf=dev"></script>
   
   <!-- SHOULD BE -->
   <script src="/dnd/shared/api-handler-base.js?v=1764054345"></script>
   <script src="/dnd/shared/campaign-base.js?v=1764054345"></script>
   <script src="/dnd/campaigns/dax/campaign-config.js?v=1764054345"></script>
   ```

2. **Fix Svelte Hash References**:
   - Both campaigns reference `index-DpGllzxI.js`
   - Actual file: `index-C8l6J5-K.js`
   - Update lines 443 (test-silverpeak) and 630 (dax)

3. **Add Missing Fonts to Silverpeak HTML** (after line 17):
   ```html
   <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700&family=IM+Fell+English&family=Crimson+Text:wght@400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
   ```

### 🟡 HIGH PRIORITY (Should Fix Soon):

4. **Migrate Dax to Shared Architecture**:
   - Remove inline CSS from HTML
   - Use shared scripts like test-silverpeak
   - Convert to Svelte components

5. **Remove Cross-Campaign Logic** (dax/index.html:614-641):
   - Dax shouldn't have test-silverpeak specific code
   - Move to campaign-config based detection

6. **Consolidate Modals**:
   - Extract 3 modals to shared components
   - Save ~300 lines per campaign

### 🟢 MEDIUM PRIORITY (Cleanup):

7. **Remove Placeholder Content** (dax/index.html:183-186)
8. **Add Character Portraits** for test-silverpeak
9. **Standardize Default Campaign IDs** (dax should default to 'dax', not 'default')

---

## FILES SUMMARY

| File | Status | Issues |
|------|--------|--------|
| `/opt/dnd/campaigns/dax/index.html` | ⚠️ Broken | Missing scripts, wrong hash, bloated |
| `/opt/dnd/campaigns/dax/theme.css` | ✅ Good | Minor duplication with inline CSS |
| `/opt/dnd/campaigns/dax/campaign-config.js` | ✅ Perfect | No issues |
| `/opt/dnd/campaigns/test-silverpeak/index.html` | ⚠️ Partial | Wrong Svelte hash, missing fonts |
| `/opt/dnd/campaigns/test-silverpeak/theme.css` | ⚠️ Incomplete | References unloaded fonts |
| `/opt/dnd/campaigns/test-silverpeak/campaign-config.js` | ✅ Good | Missing portraits only |

---

## ARCHITECTURAL NOTES

**Test-Silverpeak** represents the **future architecture**:
- Shared core scripts (`api-handler-base.js`, `campaign-base.js`)
- Svelte components for UI
- Minimal HTML with mount points
- Campaign-specific config only

**Dax** represents the **legacy architecture**:
- Inline scripts and CSS
- Full HTML for all UI elements
- No shared scripts
- Missing required files

**Migration Path**: Dax should be refactored to match test-silverpeak's architecture.

